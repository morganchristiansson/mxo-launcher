# 0x004b50dc - CLTLoginState_State13

## Overview
Family B / B1-branch helper with its own slot 2 override and a distinct margin request / reply pair. Allocated as an 8-byte object.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00439680` | specialized code | custom slot-2 override; on its special case it dispatches into this object's slot 3, otherwise falls back to `0x00438df0` |
| 3 | `0x0043bb90` | specialized code | builds a margin packet from current selection data and posts event `0x19` |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x0043bc60` | specialized code | expects opcode `0x16`; on success writes data into `DAT_004f788c`, switches state `9`, and posts event `0x1a` |
| 7 | `0x00438cd0` | tiny state-id getter | undefined code in Ghidra; returns constant `13` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x00439650` | constructor / initializer | installs vtable `0x004b50dc` |

## Key specialized methods
- `0x00439680` is another strong inheritance clue: its special path calls this vtable's slot 3, but the default path explicitly delegates back to `0x00438df0`.
- `0x0043bb90` and `0x0043bc60` form the specialized request / reply pair for this helper. The reply handler updates the `DAT_004f788c` object and transitions into mediator state `9`.
- Slot 7 (`0x00438cd0`) returns `13`.

## Composition notes
- This belongs in the same B1 derived branch as `0x004b503c` and `0x004b5064`, but it carries a later-state helper id and a different reply parser.
- The `DAT_004f788c` write in `0x0043bc60` also links it forward into the later `0x004b517c` branch.
