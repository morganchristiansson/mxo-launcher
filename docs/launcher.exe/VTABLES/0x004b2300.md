# VTable `0x004b2300` - `CLTTCPReadOperation`

## High-confidence identity

This vtable belongs to the read-buffer fragment object family passed into the connection front-end
receive seam.

Current best concrete class name:
- **`CLTTCPReadOperation`**

Evidence:
- `CLTThreadPerClientTCPEngine_WorkerThread_Run` allocates `0x100c` bytes for this object on both
  TCP and UDP receive paths
- `CMessageConnection::OnOperationCompleted` logs leftover fragment bytes as:
  - `Unused buffers were attached to CLTTCPReadOperation from %d.%d.%d.%d:%d.`
- `CLTTCPConnection::OnReceive` (`0x449d40`) and
  `CVariableLengthPrefixedTCPStreamParser::Parse` (`0x469bf0`) use this object's virtual `+0x04`
  / `+0x08` as AddRef / Release hooks

## Recovered vtable rows

Direct memory read of `0x004b2300`:

| Address | Target | Current best read |
|---|---:|---|
| `0x004b2300` | `0x0042fd50` | `CLTTCPReadOperation_dtor` |
| `0x004b2304` | `0x0042f850` | `CLTTCPReadOperation_AddRef` |
| `0x004b2308` | `0x0042f860` | `CLTTCPReadOperation_Release` |
| `0x004b230c` | `0x004199b0` | `Pointer_DeleteIfNonNull` helper |
| `0x004b2310` | `0x0042f880` | `CLTTCPReadOperation_ResetRefCount` |
| `0x004b2314` | `0x0042f890` | `CLTTCPReadOperation_SetRefCountFromPtr` |

## Recovered field layout

High-confidence layout from the worker-thread producer plus the vtable methods:

- `+0x00` = vtable
- `+0x04` = interlocked refcount
- `+0x08` = byte count
- `+0x0c` = first payload byte
- allocation size on the active worker-thread receive path = `0x100c`
  - i.e. inline storage for up to `0x1000` bytes beginning at `+0x0c`

## Method behavior

### `0x0042fd50 = CLTTCPReadOperation_dtor`

Assembly/decompile shape:
- overwrites the vtable with base `0x004b211c`
- if `(deleteFlag & 1) != 0`, frees the object through `0x452520`

Current best interpretation:
- deleting-dtor-style entry at vtable `+0x00`

### `0x0042f850 = CLTTCPReadOperation_AddRef`

```asm
add ecx, 4
push ecx
call InterlockedIncrement
```

Current best interpretation:
- interlocked increment of object `+0x04`

### `0x0042f860 = CLTTCPReadOperation_Release`

```asm
lea eax, [ecx+4]
push eax
call InterlockedDecrement
test eax, eax
jnz  return
mov edx, [ecx]
jmp [edx+0xc]
```

Current best interpretation:
- interlocked decrement of object `+0x04`
- when the count reaches zero, dispatches vtable `+0x0c`
  - on this vtable that is `Pointer_DeleteIfNonNull`
  - which in turn calls vtable `+0x00(1)`

So the zero-count release path is:
- `Release()`
- `-> Pointer_DeleteIfNonNull(this)`
- `-> deleting dtor(this, 1)`

### `0x0042f880 = CLTTCPReadOperation_ResetRefCount`

Current best interpretation:
- `InterlockedExchange(&this->refCount, 0)`

### `0x0042f890 = CLTTCPReadOperation_SetRefCountFromPtr`

Current best interpretation:
- `InterlockedExchange(&this->refCount, *valuePtr)`

### `0x00452350 = CLTTCPReadOperation_SetByteCount`

Current best interpretation:
- clamps incoming count to `0x1000`
- writes the result to object `+0x08`

## Concrete producer shape

`CLTThreadPerClientTCPEngine_WorkerThread_Run` now gives a direct static construction/use pattern.

### TCP receive path

The worker thread:
- allocates `0x100c`
- writes:
  - `vtable = 0x004b2300`
  - `refCount = 0`
  - `byteCount = 0`
- calls `CLTTCPReadOperation_AddRef()`
- receives into `object+0x0c`
- calls `CLTTCPReadOperation_SetByteCount(recvLen)`
- calls connection vtable `+0x14`
  - current best read: `CLTTCPConnection::OnReceive(readOperationFragment)`
- then drops its outer reference with `readOperationFragment->Release()`

### UDP receive path

The worker thread builds the same object family, then prepares a wider call to connection vtable
`+0x18`:
- arg1 = `readOperationFragment`
- arg2 = pointer to a copied 16-byte peer/source-address blob
- arg3 = immediate `0x004b2118`

The current inherited `CLTTCPConnection::OnClose` implementation ignores args 2/3 and only releases
arg1, but this caller proves the original callback ABI is wider than a single-stack-arg wrapper.

## Relationship to the parser seam

This object is the concrete front-end input consumed by:
- `0x449d40 = CLTTCPConnection::OnReceive`
- `0x469bf0 = CVariableLengthPrefixedTCPStreamParser::Parse`

Current best ownership model:
- worker thread holds one outer reference while delivering the fragment to the connection callback
- `OnReceive` takes another temporary reference before its first parser handoff
- `Parse` takes/releases its own transient reference while appending retained fragments into the
  completed packet work item
- the final `OnReceive` / worker-thread releases unwind the outer delivery references

## Source lockstep

Current source now mirrors this producer seam one step more faithfully even though the full original
`0x42fe50` worker-thread main body is still not reimplemented.

Updated source points:
- `matrixstaging/runtime/src/liblttcp/lttcpconnection.cpp`
- `matrixstaging/runtime/src/liblttcp/lttcpconnection.h`
- `matrixstaging/runtime/src/libltmessaging/messageconnection.cpp`
- `matrixstaging/game/src/libltclientlogin/loginmediator.cpp`

Current narrowed source behavior:
- `CLTTCPConnection::PollReceiveAndDeliverReadOperationFragmentsScaffold()` now mirrors the active
  TCP receive delivery shape from `0x42fe50`:
  - allocate a source-owned `CLTTCPReadOperation`-family fragment with `0x1000` payload capacity
  - install AddRef / Release / reset-refcount / set-refcount-from-ptr behavior matching vtable
    `0x004b2300`
  - copy received bytes into fragment `+0x0c`
  - clamp/write fragment byte count through the recovered `0x452350` behavior
  - call `OnReceive(readOperationFragment)`
  - drop the worker-held outer reference after delivery
- the live launcher bridge now uses the fragment-delivery helper again:
  - `PumpLauncherConnectionContextScaffold()` calls
    `PollReceiveAndDeliverReadOperationFragmentsScaffold()`
  - the older `ReceivedBytes()` drain remains only as a compatibility fallback beneath the same
    mediator receive handlers
- forcing the fragment-delivery helper live on the launcher bridge narrowed the old regression
  more tightly than the first experiment did:
  - the historical bad pass was **not** localized to a stable packet-boundary or late-margin batch
    mismatch on the active existing-character route
  - the first reproducible local failure sat earlier at the queue-consumer ABI seam: parsed-packet
    work items were enqueued with context=`this` even though queue consumers still dispatch through
    original-style `context->vtable[4]`
  - once source added the explicit `CBaseConnection_QueueContextScaffold` bridge and enqueue path,
    the same forced fragment-delivery receive path advanced through auth, state8/state9, and into
    game in bounded reruns
  - a later static-RE check against `0x4490c0` / `0x4350c0` then localized the remaining source
    mismatch further: parser-owned emitted work items must preserve the
    `firstRetainedFragment10 == fragment-containing-currentCursor24` invariant after prefix consume
  - source now repairs that invariant in parser/work-item bookkeeping, which is what allowed the
    launcher bridge to switch back from `PollReceiveNonBlocking()` to the original-shaped fragment
    delivery path
- downstream source lockstep therefore remains useful and locally sufficient on the active path:
  - `CLTTCPConnection::OnReceive` / parser produce queued type-3 parsed-packet work items
  - `CMessageConnection::OnOperationCompleted` copies packet-body bytes out of
    `currentCursor24/+0x28`
  - mediator-side receive handling drains those copied parsed packets before falling back to the
    older raw-buffer bridge

So the remaining open gap for `0x42fe50` is no longer “no original-shaped fragment object exists on
this path.” A fresh static-RE check against `0x4490c0` / `0x4350c0` also narrowed the parser-side
seam more tightly: emitted parsed-packet work items must preserve the original
`firstRetainedFragment10 == fragment-containing-currentCursor24` invariant after prefix consume,
rather than pushing that repair down into the consumer. With that local parser bookkeeping repair
in place, the live launcher bridge can stay on the fragment-delivery seam again.
