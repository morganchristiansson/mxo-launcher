# 0x004aae24 - provisional `LauncherLoginDialog` secondary subobject vtable

## Focused slot table

| Slot | Entry | Current read |
|---|---:|---|
| +0x00 | `0x402ba0` | hosted-window/event callback entry; handles code `0x103`, shows dialog `+0xae8` hosted window when dialog `+0xae8` is live and local gate `+0x18` is clear, then invalidates the parent dialog |
| +0x04 | `0x4028a0` | shared class-info / runtime-class-style stub |
| +0x08 | `0x403a60` | shared scalar-dtor thunk |
| +0x0c | `0x441790` | trivial stub |
| +0x10 | `0x401030` | trivial stub |
| +0x14 | `0x441790` | trivial stub |

## Current best identity

`0x004aae24` is the **final secondary vtable** installed at:

- `LauncherLoginDialog + 0x54`

Current xrefs are tight:

- ctor installs it at `0x4033c2`
- dtor reinstalls it at `0x4035b5`

So this is not a separate top-level class root.
It is a secondary subobject callback/helper surface owned by the same `LauncherLoginDialog`
instance.

## Strongest concrete method

First entry:

- `0x402ba0`

Current best read:
- checks for callback/event code `0x103`
- tests local gate byte `+0x18` (`LauncherLoginDialog + 0x6c`)
- reaches back to the primary dialog object via `this - 0x34`
- shows the embedded/browser-side window/control stored at base `+0xae8`
- invalidates the base dialog window afterward

That is strong evidence that:
- this vtable belongs to a helper subobject embedded inside the dialog
- the subobject cooperates with the embedded browser/helper window owned by the dialog
- it is not the primary page/controller vtable itself

## Relation to `0x004aadf8`

Ctor order is now important:

1. `LauncherLoginDialog_ctor` seeds dialog `+0x54` with `0x004aadf8`
2. then overwrites dialog `+0x54` with `0x004aae24`

So `0x004aadf8` is currently better read as a ctor-only seed/temporary state for the same
subobject, while `0x004aae24` is the stable secondary vtable used after construction.

## Practical consequence for class identity

This secondary-vtable evidence **supports** the current provisional `LauncherLoginDialog` class
identity because it shows:

- one primary dialog/controller object at offset `0`
- one cooperating helper/callback subobject at offset `+0x54`
- both being constructed and destructed together

It does **not** by itself provide the exact original C++ type spelling.
