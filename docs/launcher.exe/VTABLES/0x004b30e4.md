# 0x004b30e4 - CConsoleBool

## Overview
Typed boolean console-variable leaf recovered from the `consolebool.cpp` string/file cluster.

Current confidence:
- `0x00452990 = CConsoleBool_ConstructDefault` installs vtable `0x004b30e4` and then jumps into the
  shared typed-console initializer at `0x00416230`
- `0x004529e0 = CConsoleBool_ParseValue` accepts several faithful text forms:
  - decimal digits via `atoi(...) != 0`
  - `true` / `yes`
  - `false` / `no`
- successful parses forward through the shared bool setter at `0x00409730`, which writes byte
  `this+0x31`
- `0x004529a0 = CConsoleBool_FormatValue` emits `"true"` or `"false"`
- `0x00452ae0 = CConsoleBool_Dump` still uses the ConsoleInt dump format string and prints the bool
  byte as an integer

Recovered leaf field surface:
- `+0x31` = boolean value byte

## VTable
| Offset | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| `+0x00` | `0x004686b0` | shared tiny stub | current meaning unresolved |
| `+0x04` | `0x00437860` | shared tiny getter | reused constant-one helper |
| `+0x08` | `0x0044e360` | shared tiny helper | current meaning unresolved |
| `+0x0c` | `0x00432fd0` | shared tiny stub | current meaning unresolved |
| `+0x10` | `0x00432fe0` | shared tiny wrapper | current meaning unresolved |
| `+0x14` | `0x004529e0` | `CConsoleBool_ParseValue` | accepts digits / `true` / `yes` / `false` / `no` |
| `+0x18` | `0x004529a0` | `CConsoleBool_FormatValue` | writes `true` or `false` |
| `+0x1c` | `0x00433000` | shared tiny stub | current meaning unresolved |
| `+0x20` | `0x00452ae0` | `CConsoleBool_Dump` | logs the bool byte via the ConsoleInt format string |
| `+0x24` | `0x00433010` | shared tiny stub | current meaning unresolved |

## Related helpers
- `0x00409730` is the shared bool setter used by `CConsoleBool_ParseValue`
  - validates through callback slot `+0x20` when present
  - writes the byte to `this+0x31`
  - then fires optional post-write callbacks at `+0x24` and `+0x28`
- the higher-level runtime command-line surface is still the non-vtable helper:
  - `0x004165b0 = CConsoleVar_ParseCommandLine`
- practical launcher note:
  - if the replacement later adopts faithful runtime-style console parsing, bool leaves like this one
    are ready for `+VarName value` handling
    while launcher-owned auth switches should remain a separate preprocessing layer
