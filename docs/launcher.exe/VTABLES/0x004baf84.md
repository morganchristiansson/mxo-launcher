# VTable `0x004baf84` - `CVariableLengthPrefixedTCPStreamParser`

## High-confidence identity

This vtable is the stream parser object stored at connection `+0x6c` on the
`CLTTCPConnection` family.

Evidence:

- `CLTTCPConnection_ctor` (`0x44aad0`) stores its third ctor arg into connection `+0x6c`
- `FUN_00448b40` obtains that object via:
  - `CVariableLengthPrefixedTCPStreamParser_StaticAllocate` (`0x469f50`)
  - `CVariableLengthPrefixedTCPStreamParser_ctor` (`0x469b20`)
- `CLTTCPConnection::OnReceive` (`0x449d40`) calls the helper at connection `+0x6c`
- `0x469bf0` is string-backed by:
  - `CVariableLengthPrefixedTCPStreamParser::Parse(): ...`

## Recovered vtable rows

| Address | Function | Current best read |
|---|---|---|
| `0x004baf84` | `0x469ef0` | `CVariableLengthPrefixedTCPStreamParser_dtor` |
| `0x004baf88` | `0x469bf0` | `CVariableLengthPrefixedTCPStreamParser_Parse` |
| `0x004baf8c` | `0x4415c0` | parser helper used after a completed packet; exact name not yet fixed |
| `0x004baf90` | `0x4725c0` | `CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket` |
| `0x004baf94` | `0x469b40` | `CVariableLengthPrefixedTCPStreamParser_AllocatePacketBuffer` |

## Current best parser-owned state relevant to the packet object family

Only the parts directly proven by the parser / parsed-packet seam are recorded here.

| Parser offset | Current best read | Confidence |
|---|---|---|
| `+0x04` | retained reference to the fragment currently containing parser cursor `+0x08` | proven |
| `+0x08` | next unread buffered byte pointer | proven |
| `+0x0c` | total unread buffered byte count across retained fragments | proven |
| `+0x10` | byte-count state incremented by `CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`; exact downstream meaning still provisional | provisional |
| `+0x14` | current parser-owned `CParsedPacketWorkItem` | proven |

Ghidra/source lockstep note:

- current Ghidra OOAnalyzer struct for this parser now carries the same tighter prefix names:
  - `currentCursorFragmentRef04`
  - `nextUnreadBufferedByte08`
  - `unreadBufferedByteCount0c`
  - `advancedBufferedByteCount10`
  - `currentParsedPacketWorkItem14`

## Field-by-field notes for the recovered parser prefix

### `+0x04` - current-cursor fragment reference

**Write sites**

- `0x472560 = CVariableLengthPrefixedTCPStreamParser_InitState`
  - zeroes `+0x04`
- `0x469bf0 = CVariableLengthPrefixedTCPStreamParser_Parse`
  - if `+0x04 == 0`, assigns the first non-empty input fragment there through retained-pointer helper
    `0x434fa0`
- `0x4725c0 = CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket`
  - releases and clears `+0x04`
  - if unread bytes remain, replaces it with the old work item’s tail fragment
- `0x472660 = CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`
  - when cursor movement crosses into another retained fragment, updates `+0x04` to that fragment
- `0x472580 = CVariableLengthPrefixedTCPStreamParser_ClearState`
  - releases `+0x04`

**Read sites**

- `0x469bf0`
  - tests whether initial current-fragment state exists yet
- `0x472660`
  - uses `+0x04` to compute bytes remaining in the current fragment
  - compares traversal fragments against it when deciding whether cursor movement stays local or
    crosses fragment boundaries
- `0x472580`
  - releases the retained reference during clear/dtor state teardown

**Meaning**

- **proven**: retained reference to the fragment currently containing parser cursor `+0x08`

**Utility helper now named in Ghidra**

- `0x434fa0 = CLTTCPReadOperationFragmentRef_AssignRetained`
  - tiny one-pointer AddRef/Release helper over a retained fragment pointer
  - used both here on parser `+0x04` transitions and later by
    `CMessageConnection::OnOperationCompleted` stack locals while walking parsed-packet fragments

### `+0x08` - next unread buffered byte pointer

**Write sites**

- `0x472560 = CVariableLengthPrefixedTCPStreamParser_InitState`
  - zeroes `+0x08`
- `0x469bf0 = CVariableLengthPrefixedTCPStreamParser_Parse`
  - when unread buffered byte count was zero, seeds `+0x08 = inputFragment + 0x0c`
- `0x472660 = CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`
  - advances `+0x08` within the current fragment
  - or rehomes it into another retained fragment
  - or clears it to `0` when unread byte count becomes zero

**Read sites**

- `0x469bf0`
  - reads prefix bytes from `+0x08`
  - copies `+0x08` into emitted work-item `+0x24` when packet body start is known
- `0x4725c0`
  - copies parser `+0x08` into the fresh replacement work item’s `+0x24` when unread bytes remain
- `0x472660`
  - uses `+0x08` to compute bytes remaining in the current fragment and to derive the new cursor

**Meaning**

- **proven**: pointer to the next unread buffered byte in the retained fragment backing

### `+0x0c` - unread buffered byte count

**Write sites**

- `0x472560 = CVariableLengthPrefixedTCPStreamParser_InitState`
  - zeroes `+0x0c`
- `0x469bf0 = CVariableLengthPrefixedTCPStreamParser_Parse`
  - adds each non-empty input fragment’s byte count into `+0x0c`
- `0x472660 = CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`
  - subtracts consumed bytes from `+0x0c`

**Read sites**

- `0x469bf0`
  - gates prefix decode and packet-complete checks with `+0x0c`
- `0x4725c0`
  - tests whether unread bytes remain after a completed packet emit
- `0x472660`
  - uses `+0x0c` in cursor-advance decisions and clears parser `+0x08` when the resulting count is
    zero

**Meaning**

- **proven**: total unread buffered byte count across retained fragments

### `+0x10` - cursor-advance byte-count state

**Write sites**

- `0x4725c0 = CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket`
  - clears `+0x10 = 0`
- `0x472660 = CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`
  - increments `+0x10 += byteCountToConsume`

**Read sites**

- no direct read is proven inside the current focused seam (`0x469bf0`, `0x4725c0`, `0x472660`)

**Meaning**

- **provisional**: byte-count accumulator advanced alongside parser cursor movement
- do **not** overname it beyond that until a concrete read site is recovered

### `+0x14` - current parser-owned work item

**Write sites**

- `0x472560 = CVariableLengthPrefixedTCPStreamParser_InitState`
  - zeroes `+0x14`
- `0x4725c0 = CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket`
  - allocates a fresh `CParsedPacketWorkItem` and stores it at `+0x14`
- `0x469ef0 = CVariableLengthPrefixedTCPStreamParser_dtor`
  - deletes and clears `+0x14`
- `0x472580 = CVariableLengthPrefixedTCPStreamParser_ClearState`
  - deletes and clears `+0x14`

**Read sites**

- `0x469bf0`
  - appends retained input fragments into `+0x14`
  - reads/writes work-item `+0x24/+0x28` through helper family calls
  - returns `+0x14` through `outCompletedPacketWorkItem` on successful parse
- `0x4725c0`
  - treats old `+0x14` as the just-emitted work item when recovering carry-over tail state
- `0x472660`
  - traverses retained fragments through parser current work item `+0x14` when cursor advance spans
    multiple fragments

**Meaning**

- **proven**: current parser-owned `CParsedPacketWorkItem`
- phase meaning is also **proven**:
  - parser-owned assembly state before emit
  - emitted completed packet object when `Parse(...) == 0`
  - fresh replacement assembly object after `ResetAfterPacket`

## `Parse` behavior (`0x469bf0`)

A newer Ghidra type/prototype pass also tightens decompilation on the direct feeder path:

- `CLTTCPConnection::OnReceive(readOperationFragment)` now carries a typed
  `CLTTCPReadOperation`/fragment parameter
- `CVariableLengthPrefixedTCPStreamParser::Parse(readOperationFragment, outCompletedPacketWorkItem)`
  now decompiles with:
  - typed input fragment prefix (`referenceCount04`, `byteCount08`)
  - typed parser fields from the table above
  - typed parsed-packet out parameter (`CParsedPacketWorkItem**`)

Current high-confidence behavior from direct decompilation **and assembly**:

- accepts:
  - an optional read-operation buffer fragment
  - an out-pointer for the completed parsed-packet work item
- current best fragment-object shape on that path:
  - worker-thread receive vtable = `0x004b2300`
  - worker-thread receive allocation size = `0x100c`
  - `+0x04` = interlocked refcount
  - dword `+0x08` = byte count
  - bytes begin at `+0x0c`
  - vtable `+0x00` = deleting-dtor-style entry
  - vtable `+0x04` = no-arg AddRef / retain on the fragment object itself
  - vtable `+0x08` = no-arg Release on the fragment object itself
  - vtable `+0x10` = reset refcount to zero
  - vtable `+0x14` = set refcount from pointed-to value

### Call shape and ownership

The first receive call is now tightly confirmed as:

- `CLTTCPConnection::OnReceive` prepares `(readOperationFragment, &completedPacketWorkItem)`
- then calls `readOperationFragment->+0x04()`
- then calls `parser->Parse(readOperationFragment, &completedPacketWorkItem)`

Later drain calls are explicitly:

- `parser->Parse(0, &completedPacketWorkItem)`

Inside `Parse` itself, the same stack pattern appears again:

- parser first calls `readOperationFragment->+0x04()`
- then immediately calls `CParsedPacketWorkItem_AppendFragment(currentWorkItem, readOperationFragment)`
- that proves the apparent stack argument beside the retain call belongs to `AppendFragment`, not
  to the fragment virtual

Ownership meaning now narrows too:

- `Parse` takes a transient reference on the incoming fragment
- `CParsedPacketWorkItem_AppendFragment` retains any fragment that becomes part of the parser-owned
  work item
- `0x435e60` itself also ends with `if (param_1 != 0) param_1->Release()`
  - so the helper body is now proven to **consume one caller-held temporary fragment ref**
  - that narrower fact is proven even though the full end-to-end refcount reconciliation across the
    worker-thread / `OnReceive` / parser / queue-consumer seam is still open
- `Parse` then calls fragment `+0x08` on all exits
- `CLTTCPConnection::OnReceive` finally calls fragment `+0x08` once more to drop the outer
  callback-held reference

Current best caller-by-caller temporary-ref sites on the active launcher.exe path are therefore:

- worker-thread receive producer `0x42fe50`
  - one worker-owned outer ref immediately after fragment allocation/setup
  - one more delivery-temp ref immediately before `OnReceive`
  - after the callback returns, only that delivery-temp ref is dropped on the success path
- `CLTTCPConnection::OnReceive` (`0x449d40`)
  - one outer temp ref around the first `Parse(fragment, &out)` call
- `CVariableLengthPrefixedTCPStreamParser::Parse` (`0x469bf0`)
  - one transient ref around `AppendFragment`
- `CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket` (`0x4725c0`)
  - when unread bytes remain, one more transient ref on parser `+0x04` before carrying the tail
    fragment into the fresh replacement work item

### Framing behavior

`Parse`:

- accumulates stream fragments into the parser-owned current work item at parser `+0x14`
- decodes the 1-byte / 2-byte variable-length prefix format
- rejects malformed prefixes and oversized packets
  - returns `0x700000b` on invalid/oversized framing
- returns `0x7000000` when more data is still needed
- returns `0` and writes a completed `CParsedPacketWorkItem` to the out slot when a full framed
  packet is available

## New clarification: parser output object is both assembly state and completed packet

This is the key narrowed result from the current pass.

### During assembly

The parser owns a current `CParsedPacketWorkItem` at parser `+0x14`.
Incoming fragments are retained into that object via `CParsedPacketWorkItem_AppendFragment`.

### At emit time

When the decoded packet-body length at work-item `+0x28` is `<= parser+0x0c` unread buffered bytes,
`Parse`:

- writes work-item `+0x24 = parser+0x08`
  - i.e. the cursor pointer where the packet body begins in the retained fragment backing
- advances parser cursor state through
  - `0x472660 = CVariableLengthPrefixedTCPStreamParser_AdvanceBufferedCursor`
- returns the current work item through `outCompletedPacketWorkItem`
- then calls parser virtual `+0x0c` / `CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket`

### After emit

`CVariableLengthPrefixedTCPStreamParser_ResetAfterPacket` (`0x4725c0`) now proves the object-family
reuse boundary:

- saves the just-emitted work item from parser `+0x14`
- allocates a **fresh** replacement work item through parser virtual `+0x10`
  - `0x469b40 -> 0x435db0 -> 0x435090`
- releases parser `+0x04` current-fragment state
- clears parser `+0x10`
- if unread buffered bytes remain:
  - gets the old work item’s tail fragment via `CParsedPacketWorkItem_GetTailFragment`
    - that helper returns the fragment through an out slot with a temporary retained reference
  - compares that tail fragment against the now-cleared parser `+0x04`
  - stores/retains that tail fragment back at parser `+0x04`
  - releases the temporary tail-fragment reference returned by `GetTailFragment`
  - takes one more transient reference on parser `+0x04`
  - appends that fragment into the fresh replacement work item
  - sets fresh replacement work-item `+0x24 = parser+0x08`

That means the parser-emitted object is:

- **a completed packet with retained fragment backing**, and
- **the same class family as the parser’s reusable assembly-state object**

So the right answer to the old either/or question is now:

- **both, depending on phase**

## `0x472660` zero-flag branch status

A newer xref check narrows the currently relevant scope more than the earlier helper-body read did.

`ghidra_get_xrefs_to(0x472660)` currently reports only the three callers inside
`CVariableLengthPrefixedTCPStreamParser::Parse`:

- `0x469c94`
- `0x469d05`
- `0x469d8e`

And each of those pushes low-byte-nonzero `cursorAdvanceFlags == 1`.

So:

- the helper body **does** contain a low-byte-zero branch
  - `test cl, cl`
  - `setz dl`
  - boundary-sensitive traversal math follows from that
- but that zero-flag branch still has **no current launcher.exe xref**
- treat it as structurally understood enough to document, but not yet proven to matter on the
  active or near-current launcher receive path

## Why this matters for `CLTTCPConnection::OnReceive`

This sharply narrows the old vague “poll helper at `+0x6c`” reading.

`0x449d40` is not polling an anonymous socket helper there.
It is driving a concrete parser object:

- connection `+0x6c` = `CVariableLengthPrefixedTCPStreamParser`
- `OnReceive` repeatedly asks that parser for completed framed packets
- each yielded `CParsedPacketWorkItem` is then queued into the engine through `0x436820`

And the queued object is now understood more precisely than before:

- not a detached raw byte blob
- not only a one-shot packet allocation
- but a fragment-backed parsed-packet object family that also serves as the parser’s in-progress
  assembly state across packet boundaries

## Source lockstep

Current source comments now mirror this narrowing in:

- `matrixstaging/runtime/src/libltmessaging/variablelengthprefixedtcpstreamparser.h`
- `matrixstaging/runtime/src/libltmessaging/variablelengthprefixedtcpstreamparser.cpp`
- `matrixstaging/runtime/src/liblttcp/lttcpconnection.h`
- `matrixstaging/runtime/src/liblttcp/lttcpconnection.cpp`

Current code-side lockstep from this pass:

- source now owns connection `+0x6c` as a real dedicated
  `CVariableLengthPrefixedTCPStreamParser` class again instead of leaving the live parser mirror
  buried inside `lttcpconnection.cpp`
- `CLTTCPConnection::OnReceive` is thinner again and now stays connection-focused:
  - fragment outer retain
  - `parser06c_->Parse(fragment, &completedPacketWorkItem)`
  - enqueue completed work items
  - `parser06c_->Parse(nullptr, &completedPacketWorkItem)` drain
  - close-on-terminal-error branch
  - final outer fragment release
- `variablelengthprefixedtcpstreamparser.cpp` now owns the active parser/object-family seam:
  - parser ctor / dtor / `Parse` / `ResetAfterPacket` / `AllocatePacketBuffer`
  - parsed-packet traversal helpers
  - parsed-packet release helper
  - parser-prefix field ownership/comments for `+0x04/+0x08/+0x0c/+0x10/+0x14`
- the live source parser still mirrors the proven active subset of
  `0x469bf0 -> 0x472660 -> 0x4725c0`:
  - variable-length prefix decode with the original `0`, `0x7000000`, `0x700000b` result split
  - packet-body emits backed by retained fragments
  - post-emit replacement-work-item carry-over through tail-fragment reuse
  - prefix-consume rebase preserving
    `firstRetainedFragment10 == fragment-containing-currentCursor24`
- one local ownership seam remains explicitly open and should **not** be overstated as solved:
  - source now restores the original helper's trailing `0x435e60 -> Release(param_1)` behavior
  - source also now mirrors the extra TCP worker-thread delivery ref from `0x42fe50`
    closely enough that the faithful helper body survives the live path again
  - but the exact full refcount reconciliation across
    `0x42fe50 -> 0x449d40 -> 0x469bf0 -> 0x435e60 -> queue consumer` is still not fully closed
  - remaining source-owned differences are narrower now:
    - `CParsedPacketWorkItem_AppendFragmentScaffold(...)` still keeps source-owned bool failure
      reporting and current list/count scaffolding
    - the prefix-consume rebase helper that preserves
      `firstRetainedFragment10 == currentCursor24-fragment` is still source-owned
  - treat those remaining details as evidence gaps still to narrow, not as settled proof that the
    whole source path is already byte-for-byte ownership-equivalent
