# 0x004b51e0 - CLTLoginState_State0

## Overview
Concrete final-leaf variant built on the `0x004b51b8` shape. Allocated directly by the dispatch-table init as an 8-byte object with byte `+4 = 0`.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00441790` | tiny stub | inherits the empty slot-3 surface from the abstract-style parent shape |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004397e0` | specialized code | same shared final-leaf handler as `0x004b51b8` |
| 7 | `0x00437b50` | tiny getter stub | returns `0` (`CLTThread_NoOp` in current Ghidra labeling) |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437b40` | tiny getter stub | returns `0` |
| 10 | `0x00439d80` | shared initializer | still points at the `0x004b51b8` constructor, so construction alone does not distinguish this variant |

## Key specialized methods
- Its specialized behavior is almost entirely encoded by the **combination** of slot 6 (`0x004397e0`), slot 7 (`0x00437b50` returns `0`), and the dispatch-table initialization byte `this+4 = 0`.
- Because slot 10 still uses `0x00439d80` (which installs `0x004b51b8`), this variant seems to be selected by direct vtable installation in the dispatch table rather than by a unique constructor.
- This makes it look much more like a concrete **state specialization** than a traditional standalone class.

## Composition notes
- This is one of the live leaves hanging off the abstract-style `0x004b51b8` node.
- The common family theme here is still “tiny `CLTLoginState` state object”, not `LaunchPadClient` in the broad sense.

