# 0x004b5208 - CLTLoginState_State3

## Overview
Another concrete final-leaf variant built on the `0x004b51b8` shape. Also allocated directly as an 8-byte object with byte `+4 = 0`.

Practical current read:
- this is the **state3 wait/helper-id leaf** on the proven happy path
- live original runs show it sitting as the current helper while the owner-side
  selection-context writers `0x41c390/0x41c1f0` consume input and advance into states `7/8`
- current evidence still does **not** justify inventing a state3-local slot-3 body

Provisional better-name suggestion:
- `CLTLoginState_SelectionContextPending`
- rationale: live original runs now show state `3` as the current helper immediately before the
  owner-side selection-context writers `0x41c390/0x41c1f0` advance into states `7/8`

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00441790` | tiny stub | shared stub; current evidence keeps state3 as a wait/helper-id leaf rather than a slot-3-owned advance |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004397e0` | specialized code | same shared final-leaf handler |
| 7 | `0x00438cf0` | tiny state-id getter | undefined code in Ghidra; returns constant `3` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437b40` | tiny getter stub | returns `0` |
| 10 | `0x00439d80` | shared initializer | same constructor target as `0x004b51b8` |

## Key specialized methods
- The main differentiator here is slot 7: `0x00438cf0` is the tiny undefined-in-Ghidra getter that returns `3`.
- Slot 3 remains the inherited shared stub at `0x00441790`; the currently proved transitions out of this helper belong to owner-side mediator methods, not to a state3-local body.
- Like `0x004b51e0`, it reuses the same slot-6 handler (`0x004397e0`), the same zero-return slot 9 stub, and the same shared constructor target.
- That combination makes this a concrete **state-code specialization**, not a wholly separate behavioral class.

## Composition notes
- This is another live leaf under the `0x004b51b8` abstract-style node.
- newer runtime+owner evidence now makes its practical role less generic than the old `State3` name:
  - live original launches reach this as the current helper immediately before
    `0x41c390 = CLTLoginMediator_SetSelectionIndexAndSwitchToState7`
    and `0x41c1f0 = CLTLoginMediator_PersistSelectionContextAndSwitchToState8`
  - corrected happy-path WineDbg now tightens the lead-in too:
    - after the early auth chain `state0 -> state2 -> state1 -> state2`, the next proven switch is
      `state2 -> state3`
    - `0x41c1f0` is then reached while owner `+0x10` still points at vtable `0x004b5208`
    - a second fresh rerun with the correct password again reached `0x41c1f0` directly from that
      state-`3` wait period, with no additional early helper-switch hits in the narrow breakpoint set
  - static owner-side decompilation now matches that practical read too:
    - `0x41c390` first checks current state code `> 2`, stores the byte at owner `+0xcc8`, then
      switches helper state to `7`
    - `0x41c1f0` first checks current state code `> 2`, copies the full `0xb4` snapshot into
      owner `+0xcc8/+0xcd0..+0xd7f`, then switches helper state to `8`
  - current best interpretation therefore stays narrow:
    state3 is the waiting helper identity, while the owner-side methods own the practical advance
  - that is why `CLTLoginState_SelectionContextPending` is now a reasonable provisional alias
- Its only meaningful concrete override relative to the sibling leaves is the slot-7 state-id getter.
