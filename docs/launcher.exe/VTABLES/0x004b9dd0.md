# 0x004b9dd0 - StreamTransformationFilter

## Overview

`0x004b9dd0` is the vtable installed by `0x00454310 = StreamTransformationFilter_ctor`.

Current evidence:

- constructor `0x00454310` installs this vtable and stores the underlying stream-transformation object at `this+0x4c`
- `0x00454520` logs exact `StreamTransformationFilter` padding/ciphertext error strings from `filters.cpp`
- the object shape and behavior match a block-cipher stream/padding filter that owns final-block handling

## Confirmed entries

| Offset | Entry Address | Current symbol | Notes |
|---|---:|---|---|
| `+0x00` | `0x004543d0` | `StreamTransformationFilter_dtor` | destructor / cleanup path |
| `+0xd4` | `0x00454410` | `FUN_00454410` | helper used by the filter path; exact original name still unresolved |
| `+0xd8` | `0x00453400` | `FUN_00453400` | helper used by the filter path; exact original name still unresolved |
| `+0xdc` | `0x00454520` | `StreamTransformationFilter_LastPut` | final-block / padding handler |

## Key recovered behavior

### `0x00454310 = StreamTransformationFilter_ctor`

- derives block/buffer sizing from the underlying transformation object
- installs vtable `0x004b9dd0`
- stores the attached downstream transformation/filter
- records the requested padding mode at `this+0x50`

### `0x00454520 = StreamTransformationFilter_LastPut`

High-confidence behavior:

- performs the final encryption/decryption transform
- handles block padding insertion/removal
- validates PKCS#7 padding on decrypt
- rejects ciphertext whose final length is not compatible with the block size
- emits exact log strings:
  - `StreamTransformationFilter: ciphertext length is not a multiple of block size ...`
  - `StreamTransformationFilter: invalid PKCS #7 block padding found.`

## Confidence

High for:

- class identity `StreamTransformationFilter`
- `0x00454310` as the constructor for this vtable
- `0x00454520` as the filter's final-block / padding handler

Lower confidence for:

- exact names of the other vtable entries not yet recovered from stronger string/call-shape evidence
