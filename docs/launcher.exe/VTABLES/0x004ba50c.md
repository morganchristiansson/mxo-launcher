# AuthBootstrap680BigIntObjects_0x4ba50c VTable

VTable address: `0x004ba50c` (within each big-int object at offsets +0xb0, +0xc4, +0xd8)

**Class**: `AuthBootstrap680BigIntObjects_0x4ba50c` (20-byte RSA big-int object)

**Source**: `matrixstaging/game/src/libltclientlogin/authbootstrap680_internal.h`

## Class Structure

```cpp
struct AuthBootstrap680BigIntObjects_0x4ba50c {
    uint32_t vtable00 = 0u;           // +0x00, set to 0x4ba51c after init
    uint32_t reserved04 = 0u;         // +0x04
    uint32_t capacityWords08 = 0u;   // +0x08
    void* digits0c = nullptr;         // +0x0c
    uint32_t sign10 = 0u;             // +0x10
};
static_assert(sizeof(AuthBootstrap680BigIntObjects_0x4ba50c) == 0x14u);
```

- **Size**: 20 bytes (0x14)
- **Three instances** embedded in `AuthBootstrap680Child`:
  - +0xb0: `modulusBigIntB0`
  - +0xc4: `publicExponentBigIntC4`
  - +0xd8: `privateExponentBigIntD8`

## VTable

| Offset | Address | Notes |
|--------|---------|-------|
| +0x00 | 0x4ba50c | RTTI |
| +0x04 | (unused) | |
| +0x08 | 0x45d660 | dtor ~cls_0x4ba6b0 |

Note: After construction, vtable is set to `0x4ba51c`.

## Constructor

- **0x45d000** = initializes the big-int object

## Usage

Used for RSA key material in auth bootstrap:
- Modulus big-int at +0xb0
- Public exponent at +0xc4
- Private exponent at +0xd8

Materialized by auth-reply handler at `0x448140`.
