# 0x004b8438 - stack/base message-ref sink in the helper-local transformed-output family

## Current best read
This table is part of the small transformed-output sink family used by both:
- `0x44d500 = CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch`
- `0x44d390 = CStreamPacketEncryptionModule_WriteHelper_TransformAndDispatch`

Current strongest construction anchor is the stack-local object built in `0x44d500`:
- `FUN_00437900(&local_18, 0)`
- temporary vtable assignment `0x004b8438`
- `local_10 = createdMessageRef`
- `AddRef(createdMessageRef)`
- then final retable to `0x004b84f0`

So `0x004b8438` currently reads best as the **stack/base construction-stage sibling** of the final sink table `0x004b84f0`, not as the long-lived pooled final table.

## Recovered behavior
Shared sink behavior in this family:
- field `+0x08` holds the output message-ref object
- slot `+0x14` (`0x44c8b0`) writes transformed bytes directly into that message-ref's append pointer and then grows its payload length through the message-ref object
- slot `+0x20` (`0x44c8f0`) delegates through `0x41bb60` with a local zero dword, i.e. a tiny output-length reset/flush-style helper

What distinguishes `0x004b8438` from `0x004b84f0` is the destructor row:
- `0x44c880` releases field `+0x08`
- then falls back to ordinary free (`0x409790`) when delete flag bit 0 is set

That fits a **base / stack-safe construction vtable** that is briefly installed before the final table takes over.

## Relationship to `0x004b84f0`
These two tables share the same practical sink surface except for slot `+0x00`:
- `0x004b8438`
  - generic/freeing dtor path (`0x44c880`)
- `0x004b84f0`
  - pooled dtor path (`0x44d0f0`)

So current best family read is:
- `0x004b8438` = temporary/base sink vtable during construction
- `0x004b84f0` = final sink vtable actually used by the helper-local transform path

## VTable highlights
| Offset | Function | Current best read |
|---:|---:|---|
| `+0x00` | `0x44c880` | generic/freeing dtor; releases output message-ref |
| `+0x14` | `0x44c8b0` | write transformed bytes into output message-ref |
| `+0x20` | `0x44c8f0` | tiny zero-length/reset delegate via `0x41bb60` |
