# 0x004b4fc4 - CLTLoginState_State1

## Overview
Best current concrete candidate for the missing **state `1`** login-state object.
This is a 4-byte object in the helper dispatch table and sits directly on the early auth-connect path.

## Why this is now a state-1 match
- slot 3 is `0x00439090 = CLTLoginMediator_Helper1_StartAuthConnection`
- that body caches the upstream object at `this+4` and calls `CLTLoginMediator_BeginAuthConnection`
- slot 7 is `0x0044e360`, which is the tiny `mov eax,1 ; ret` stub
- slot 10 is `0x00439060`, which installs vtable `0x004b4fc4`

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x004390b0` | specialized code | custom gate; on one branch it falls back to `0x00438d80`, otherwise it updates mediator state/error and may dispatch through this object's slot 3 |
| 2 | `0x00438df0` | shared gate | common slot 2 handler |
| 3 | `0x00439090` | specialized code | `CLTLoginMediator_Helper1_StartAuthConnection`; caches upstream pointer and calls `CLTLoginMediator_BeginAuthConnection` |
| 4 | `0x00441790` | tiny stub | trivial `ret` |
| 5 | `0x004397c0` | tiny shared stub | writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x00439190` | tiny shared stub | writes `DAT_004f78b8+0x80 = 0x12000005`, returns false |
| 7 | `0x0044e360` | tiny state-id getter | `mov eax,1 ; ret` |
| 8 | `0x00441790` | tiny stub | trivial `ret` |
| 9 | `0x00437860` | tiny getter stub | returns `1` |
| 10 | `0x00439060` | constructor / initializer | installs vtable `0x004b4fc4` |

## Key specialized methods
- `0x00439090` is the strongest identity anchor: it is the helper-1 auth-connect start body.
- `0x004390b0` makes this look like an early concrete state leaf rather than just a raw dispatch-table entry because it explicitly mixes shared-gate fallback with state-specific mediator/error handling.
- `0x0044e360` closes the loop by proving that slot 7 returns state id `1`.

## Composition notes
- This fills the previously missing concrete **state `1`** position between `CLTLoginState_State0` and `CLTLoginState_AuthenticatePending`.
- Structurally it fits the same Family B auth/bootstrap branch as the other early auth-side helpers.
