# 0x004ae6d8 - CConsoleString

## Overview
Typed string console-variable leaf recovered from the `consolestring.cpp` string/file cluster.

Current confidence:
- `0x004183b0 = CConsoleString_Construct` builds the leaf from name / default-value / help text
  inputs, then installs vtable `0x004ae6d8`
- `0x004180a0 = CConsoleString_DestroyBuffers` frees the string-owned buffers and then tails into the
  shared typed-console cleanup
- `0x00418200 = CConsoleString_AssignValue` owns the real string write path
  - validates through callback slot `+0x20` when present
  - reallocates the live buffer when needed
  - copies the incoming text into the live buffer at `this+0x34`
  - updates visible string length at `this+0x3c`
  - then fires optional post-write callbacks at `+0x24` and `+0x28`
- `0x00418060 = CConsoleString_FormatValue` emits a quoted `"%s"` from the live buffer
- `0x004182f0 = CConsoleString_Dump` logs `"%s [%X]: %s (ConsoleString)\n"`

Recovered leaf field surface:
- `+0x30` = default string buffer
- `+0x34` = live string buffer
- `+0x38` = allocation size / capacity for the live buffer
- `+0x3c` = visible string length

## VTable
| Offset | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| `+0x00` | `0x00418380` | `CConsoleString_Destruct` | destroys buffers; optional self-free on bit-0 flag |
| `+0x04` | `0x00418150` | tiny getter | returns constant `2` |
| `+0x08` | `0x00437b40` | shared tiny getter | reused constant-zero helper |
| `+0x0c` | `0x00418160` | tiny getter | returns `this+0x3c + 1` when a live buffer exists |
| `+0x10` | `0x00418170` | tiny getter | returns the live buffer pointer at `this+0x34` |
| `+0x14` | `0x00418180` | vtable tail-call wrapper | indirect tail-call to vtable `+0x2c`; exact recovered signature still needs cleanup |
| `+0x18` | `0x00418040` | wrapper | forwards one argument to vtable `+0x2c` and reports success |
| `+0x1c` | `0x00418060` | `CConsoleString_FormatValue` | writes quoted string text from `this+0x34` |
| `+0x20` | `0x00418190` | equality helper body at `0x00418190` | compares default buffer `+0x30` against live buffer `+0x34` |
| `+0x24` | `0x004182f0` | `CConsoleString_Dump` | logs the current string value |
| `+0x28` | `0x004181f0` | helper | forwards default buffer `this+0x30` into vtable `+0x18` |
| `+0x2c` | `0x00418200` | `CConsoleString_AssignValue` | real string assignment path |

## Notes for reimplementation
- This leaf is one of the clearest examples that the original console system separates:
  - high-level command-line/config parsing in `consolevar.cpp`
  - typed value storage / formatting / dump behavior in the individual leaf files
- That split is useful for the replacement launcher too:
  - launcher-owned auth stripping can stay launcher-specific
  - runtime-style `+VarName value` handling can later be moved toward this recovered console system
