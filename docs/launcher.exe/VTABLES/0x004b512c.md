# 0x004b512c - CLTLoginState_State10

## Overview
Family B helper for the auth-reply handoff into the post-auth margin stage. Only 4 bytes wide in the dispatch table.

Provisional better-name suggestion:
- `CLTLoginState_AuthReplyPending`

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x0043bf90` | specialized code | sends a packet containing mediator string data at `+0x108`, then posts event `0x13` |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004401a0` | specialized code | handles auth reply opcode `0x0b`; non-`0x0b` writes owner `+0x80 = 0x12000005`, error replies switch to state `3` and post error `0x0b`, success writes owner-managed state tables, switches to state `0x0b`, and posts event `0x14` |
| 7 | `0x00438ca0` | tiny state-id getter | undefined code in Ghidra; returns constant `10` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x004396f0` | constructor / initializer | installs vtable `0x004b512c` |

## Key specialized methods
- `0x004401a0` is one of the best anchored methods in the whole branch: it is the auth-reply handler that allocates the next state object, copies a string into owner-managed storage, then explicitly switches state to `0x0b` and posts event `0x14`.
- newer direct `0x4401a0` review also tightens the side branches:
  - non-`0x0b` packets are rejected in-place with owner `+0x80 = 0x12000005`
  - error replies do **not** continue into helper11/state11
  - instead they switch helper state to `3` and post error `0x0b`
  - only successful replies perform the slot-record writeback, helper11 switch, and event `0x14`
- `0x0043bf90` is now re-read as a gated small packet-builder/send body:
  - first gate: `0x41b4b0`
    - owner `+0x1c` must be non-null
    - connection state field `+0x34` must equal `2`
    - otherwise original switches helper state to `4`
  - second gate:
    - owner byte `+0xf14` must be non-zero
    - otherwise original switches helper state to `6`
  - packet-builder family:
    - `0x43a1f0` initializes a tiny raw-`0x0a` packet object
    - `0x43aa80` copies owner `+0x108` into the packet's `CharacterName` field
  - then it sends through `0x41af70` and posts event `0x13`
- source ownership consequence:
  - the active scaffold now keeps that slot-3 body in
    `matrixstaging/game/src/libltclientlogin/loginstate_state10.cpp`
    as `CLTLoginState_State10::Slot3_BeginOrContinue`
  - slot 6 ownership is now kept there too:
    - `CLTLoginState_State10::Slot6_HandleSecondaryMessage`
      is the current source-owned entry that consumes staged `AS_AuthReply` bytes,
      rejects non-`0x0b`, mirrors the error-vs-success branch, runs the narrower
      mediator-side parse/adopt helper, and advances into helper11 only on success
  - the local packet-builder family is source-owned there too as:
    - `0x43a1f0 = CLTLoginMediatorPacket0x0a_ResetAndInitialize`
    - `0x43aa80 = CLTLoginMediatorPacket0x0a_SetCharacterName`
  - mediator only keeps the narrow shared gates/transport/staging helpers:
    - `0x41b4b0` connection-state check
    - `0x41af70` current-connection send helper
    - staged incoming auth-packet bytes plus owner-state writeback scaffolding
- Slot 7 (`0x00438ca0`) returns `10`, which matches the idea that this is the state immediately before the post-auth state `11` stage.

## Composition notes
- This is the clearest bridge into the post-auth subchain `0x004b512c -> 0x004b5154 -> 0x004b517c -> final leaves`.
- That makes it an important **intermediate pipeline node**, even though it is still a concrete dispatch-table entry rather than a synthetic root class.
