# VTable `0x004b3e08` - provisional `CParsedPacketWorkItem`

## High-confidence identity

This vtable is the completed-packet work-item object family emitted by
`CVariableLengthPrefixedTCPStreamParser::Parse` and later queued by `CLTTCPConnection::OnReceive`.

Evidence:

- parser allocator path:
  - `0x469b40 = CVariableLengthPrefixedTCPStreamParser_AllocatePacketBuffer`
  - allocates through `0x435db0`
  - constructs through `0x435090 = CParsedPacketWorkItem_ctor`
- ctor writes:
  - vtable `0x004b3e08`
  - work type `+0x04 = 3`
  - low byte at `+0x18 = 1`
- parser helper family then uses object fields:
  - `+0x10` first fragment ref
  - `+0x14` optional fragment list
  - `+0x1c` traversal state
  - `+0x24` current cursor state
  - `+0x28` assembled byte count

## Visible vtable rows

Current visible table body from direct memory read:

| Address | Target | Current best read |
|---|---:|---|
| `0x004b3e08` | `0x435f30` | `CParsedPacketWorkItem_dtor` |
| `0x004b3e0c` | `0x4199b0` | `Pointer_DeleteIfNonNull` |

## Important helper family around this object

These are not vtable rows, but they are the current best field-semantic anchors for the object:

- `0x434f60 = CParsedPacketWorkItem_GetAssembledByteCount`
  - returns `self+0x28`
- `0x434f70 = CParsedPacketWorkItem_SetCurrentCursor`
  - writes `self+0x24`
- `0x434f80 = CParsedPacketWorkItem_GetCurrentCursor`
  - returns `self+0x24`
- `0x434f90 = CParsedPacketWorkItem_SetAssembledByteCount`
  - writes `self+0x28`
- `0x4350c0 = CParsedPacketWorkItem_BeginFragmentTraversal`
  - starts traversal from first fragment `+0x10`
- `0x435510 = CParsedPacketWorkItem_GetNextFragment`
  - iterates the retained fragment chain
- `0x4355c0 = CParsedPacketWorkItem_GetTailFragment`
  - returns the tail fragment in the current retained fragment state
- `0x435e60 = CParsedPacketWorkItem_AppendFragment`
  - appends/retains a fragment into the work-item fragment state
- `0x435dd0 = CParsedPacketWorkItem_ResetFragmentState`
  - clears retained fragment state
- `0x435d20 = CParsedPacketWorkItem_ClearFragmentList`
  - releases nodes in the retained fragment list

## Why this matters

This narrows the receive bridge substantially:

- parser output from `connection+0x6c` is not a raw byte blob
- it is a retained fragment-backed packet work item with its own traversal state
- `CLTTCPConnection::OnReceive` then pushes that packet work item into the engine queue via `0x436820`

So the current source-owned packet-work scaffold in `lttcpconnection.h` should be treated as a
fragment-backed parsed-packet object, not just a generic "type-3 packet" placeholder.
