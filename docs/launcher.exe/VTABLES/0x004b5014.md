# 0x004b5014 - CLTLoginState_AuthenticatePending

## Overview
Family B state object for an earlier auth/bootstrap stage. The object is 8 bytes wide because it caches an upstream pointer at `this+4`.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `CLTThreadPerClientTCPEngine_WorkItemHeader_GetWorkType`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00439210` | specialized code | begins an auth-bootstrap request and caches the upstream object unless its slot `+0x18` already returns `1` |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x0043f300` | specialized code | large auth reply / world+character materialization handler |
| 6 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 7 | `0x00418150` | tiny state-id getter | undefined code in Ghidra; returns constant `2` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x004391e0` | constructor / initializer | installs vtable `0x004b5014` |

## Key specialized methods
- `0x00439210` clearly belongs to the state2 login-state handoff into the owner `+0x680`
  bootstrap child:
  - if auth is **not** connected yet, it switches back to helper/state `1`
  - if auth **is** connected, it gathers owner data and calls `AuthBootstrap680_PrepareAndDispatch(...)`
- `0x0043f300` is one of the largest methods in this whole family; it does **not** parse raw auth wire packets by itself.
  Instead it first calls the separate owner `+0x680` child helper `0x448140 = AuthBootstrap680_HandleInboundAuthMessage`, then interprets that helper's return code to:
  - mirror child status into owner `+0x80`
  - build owner-managed world/character arrays
  - normalize invalid status/type values
  - post events `5` / `6` or errors depending on the return code
  - important boundary consequence from the newer `0x43f300/0x448140` decompilation:
    - both functions consume a higher-level incoming auth-message object
    - `0x448140` begins by reading the opcode from that object through `0x41bc20`
    - transition consequence for source:
      - keep the incoming auth-message object alive through state2 / `0x448140`
      - any replacement-only raw payload sidecar should be copied deeper there after the
        `0x41bc20`-style decode, not at auth leaf `0x449a30`
  - current source ownership consequence:
    - early inbound raw `0x07/0x09` and the broader bootstrap-side raw `0x0b` now route into
      `matrixstaging/game/src/libltclientlogin/loginstate_state2.cpp`
      `CLTLoginState_AuthenticatePending::AuthMessageDispatch(...)`
      plus `matrixstaging/game/src/libltclientlogin/authbootstrap680.cpp`
      `AuthBootstrap680Child::HandleInboundAuthMessage(...)`
    - the current replacement mirrors the decisive owner `+0x80` / helper-switch / error-or-event-`0x05` flow there
    - newer `0x43f300` decompile tightening also matters for the helper restore itself:
      after normalizing cached-upstream phase `0/0x10 -> 3`, the original calls
      `0x41b450(nextHelperStateId)` directly and lets that body load `[id*4 + 0x4f7868]`; there is
      no separate mediator-side helper-object lookup helper on this site
    - the successful-auth writeback in `0x43f300` is still inline in the binary:
      it rebuilds owner `+0xd84`, validates world status/type with helper predicates,
      stores child `+0x114/+0x118`, resets/rebuilds owner `+0x688`, normalizes
      character status values `> 6` to `7`, seeds owner `+0x818`, persists
      characters, posts event `6`, then copies the reply string / opaque blobs
    - current source mirrors that resulting owner state in
      `matrixstaging/game/src/libltclientlogin/loginstate_state2.cpp`
      `CLTLoginState_AuthenticatePending_0x4b5014::AuthMessageDispatch(...)`
      with the per-entry normalization routed through recovered-record helpers instead of
      the launcher's heap-backed temp records
    - newer source now also mirrors the neighboring success-side subset more faithfully split by the original one-time gate:
      - pre-gate
        - `0x441330 = AuthBootstrap680_SetPromptPasswordF8AndSecurIdFlag`
        - child `+0xf8`
        - child `+0x104`
        - child `+0x110` from the parsed success-header dword at payload offset `0x07`
      - gated once by the current `DAT_004f79e0`-style source mirror
        - broader owner `+0xd84/+0x688/+0x818` materialization
        - `0x441260 = AuthBootstrap680_StoreField114AndTimestamp118`
        - one-time `PostEvent(6)`
        - `0x41e760 = CLTLoginMediator_PersistCharactersIni`
        - owner `+0x94 + 0x00` through `0x43d480 = AuthBootstrap680_CopyReplyString54`
        - `0x441170 = AuthBootstrap680_CopyOpaqueReplyBlobs108_10c`
    - current strongest replacement-side candidate maps child `+0x108/+0x10c` to the parsed auth-signature / encrypted-private-exponent blob pair, but that exact pairing stays provisional
    - current remaining uncertainty is mainly on the exact semantics of child `+0x108/+0x10c/+0x110/+0x114`, not on whether those neighboring side effects exist
- Slot 7 (`0x00418150`) is a tiny getter returning `2`, which fits the idea that this helper is an early auth-side state.

## Practical state-flow note
On the current startup path this looks less like “the very first auth state” and more like the
**post-submit login-state handoff into the owner `+0x680` bootstrap child**.

Corrected happy-path WineDbg from a stable EULA attach now proves this early sequence concretely:
- mediator init installs state `0` as the initial current helper
- password submit (`0x41ecd0`) switches `state0 -> state2`
- `0x439210` is entered on state `2` with upstream state `0`
- state `2` then switches `state2 -> state1`
- after the auth-connect side, state `1` switches back `state1 -> state2`
- `0x439210` is re-entered on state `2` with upstream state `1`
- the next switch is `state2 -> state3`

So state `2` is now best read as the handoff point between:
- owner-side password submit
- state `1` auth-connect bringup
- the owner `+0x680` phase-2 auth/bootstrap child
- and the later state `3` selection-context continuation

Current source-level consequence:
- launcher-side auto-login should prefer entering this chain through owner vtable `+0x24`
  `0x41ecd0 = ProcessLoginRequest`, because that is the anchored API that accepts the submitted
  username/password block, copies it into owner `+0x94`, clears owner `+0xf4` on the default
  branch, and then performs the observed `state0 -> state2 -> state1 -> state2` handoff
- a direct synthetic jump into state `2` without first populating owner `+0x94` is therefore a
  lower-fidelity fallback only
- current source also now stops using the separate `SetAuthCredentials(...)` stash as the normal
  `0x448050` username/password feeder; the bounded fallback, when needed, comes from the same
  wrapper-facing auth-name/auth-password chain that launcher startup config already seeds
- launcher startup now owns the active pre-client auth/character-selection bridge around this
  state-2 handoff
- current replacement no longer restarts auth after `InitClientDLL`; the text-mode path now relies
  only on the earlier pre-client page-6 submit analogue that enters through `0x408400 -> +0x30 ->
  0x41ecd0`

## Composition notes
- This is best described as an **auth/bootstrap login-state object**.
- Compared with `0x004b4fec`, it sits earlier in the pipeline and performs much heavier bootstrap-side materialization work.
