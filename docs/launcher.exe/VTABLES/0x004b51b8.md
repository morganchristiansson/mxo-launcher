# 0x004b51b8 - CLTLoginState_AbstractFinalLeafBase

## Overview
Best current candidate for the **abstract intermediate** above the final three concrete leaf variants. It is not allocated directly by `CLTLoginMediator_InitializeHelperDispatchTable`.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00441790` | tiny stub | no concrete slot-3 behavior here |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004397e0` | specialized code | shared final-leaf handler; optionally calls `0x0041c5c0`, otherwise writes `0x12000005` |
| 7 | `0x0048bc34` | purecall thunk | import thunk to `purecall`; strong sign that this slot is abstract / intentionally unimplemented here |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437b40` | tiny getter stub | returns `0` |
| 10 | `0x00439d80` | constructor / initializer | installs vtable `0x004b51b8` |

## Key specialized methods
- `0x004397e0` is the only real shared behavior here: if the object byte at `this+4` is `1`, it delegates to `0x0041c5c0`; otherwise it just writes `0x12000005` to the mediator error slot.
- Slot 7 is the most important clue: `0x0048bc34` is the `purecall` thunk, so this slot is intentionally abstract / unimplemented in this variant.
- Slot 9 (`0x00437b40`) returns `0`, unlike the earlier Family B leaves that reuse the slot-9 true stub `0x00437860`.

## Composition notes
- Because the dispatch-table init allocates `0x004b51e0`, `0x004b5208`, and `0x004b5230` directly but **not** `0x004b51b8`, this vtable is best read as an abstract or default intermediate rather than a live concrete state object.
- This is one of the strongest pieces of evidence for an intermediate class inside the larger composition, even though there is still no single concrete root vtable.
