# 0x004b0c28 - CLTLoginState_State19

## Overview
Best current candidate for the **Family A intermediate**. It keeps the shared Family A surface but has its own unique slot 3, slot 8, slot 10, and state-id getter.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x004209e0` | specialized code | small undefined-in-Ghidra code stub that directly calls `MessageBoxA`; paired with the PIN messaging logic in slot 8 |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004208e0` | shared Family A handler | wraps `0x0041c5c0`; on positive result writes `0x12000005` to `DAT_004f78b8+0x80` |
| 7 | `0x00420350` | tiny state-id getter | undefined code in Ghidra; returns constant `0x13` |
| 8 | `0x00420a00` | specialized code | shows PIN accepted / rejected message boxes; on accept it switches state back to `0` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x004209b0` | constructor / initializer | installs vtable `0x004b0c28` |

## Key specialized methods
- `0x00420a00` is the clearest specialization: it is purely UI/reporting logic for the new-PIN flow and does not try to run the heavier workflow used by other Family A leaves.
- `0x004209e0` is also specialized even though Ghidra has not created a function there yet; the raw bytes show a direct `MessageBoxA` call and `ret 4`.
- Slot 7 (`0x00420350`) returns `0x13`, so this helper has its own fixed mediator-state identity.

## Composition notes
- This is the strongest evidence-backed **intermediate-style** Family A class: it is allocated as its own dispatch-table entry and provides the shared PIN-result / message surface that the other PIN helpers sit beside.
- There is still **no standalone root vtable** in this cluster; the common base has to be reconstructed from shared slots, not from a concrete root object.
