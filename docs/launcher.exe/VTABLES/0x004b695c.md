# 0x004b695c — launcher-owned auth-bootstrap helper wrapper vtable

Implemented/proven launcher wrapper state used at auth-bootstrap child `+0x54`.

## Class mapping
- vtable: `0x004b695c`
- launcher wrapper role: auth-bootstrap helper / transform wrapper
- embedded RNG subobject at `+0x04`: `0x004b68a8 = CryptoPP::RandomPool`
- embedded BT subobject at `+0x08`: `0x004b41e0 = CryptoPP::BufferedTransformation`

## Function mapping
| Address | Method |
|---|---|
| `0x4686e0` | wrapper construct with pool size `0x180` |
| `0x445500` | auth-bootstrap child ctor call site |
| `0x468640` | helper byte-generation call used by raw-`0x08` path |

## Notes
- launcher-specific wrapper, not a pure Crypto++ class
- source models the RNG semantics directly while preserving the wrapper boundary comments
