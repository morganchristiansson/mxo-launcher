# 0x004b6a60 - CMarginConnectionBootstrapPrepStateA0 base

Current best identity:
- immediate base-vtable sibling under the same prep-object family as `0x004b6ae0`
- constructor body: `0x442e20 = cls_0x4b6a60::cls_0x4b6a60`
- destructor body: `0x442df0 = ~cls_0x4b6a60`

## Constructor-side role

- seeds the shared outer object shell later retabled by `0x443220`
- constructs the inner `+0x0c` crypto/precompute subobject before the derived ctor calls
  `0x465d70`
- installs the shared outer helper-table fields around:
  - `+0x04`
  - `+0x08`
  - `+0xd0`
  - `+0xd4`
  - `+0xd8`
  - `+0xdc`

## Destructor-side role

- destroys the same inner `+0x0c` subobject family first
- then retables the remaining base shell
- `0x443390` finally restores outer `+0xd4` after this base-dtor body returns

## Family rows vs active-path proof

Shared family rows present under the broader `0x004b6a18 / 0x004b6a60 / 0x004b6ae0` cluster:
- `0x4430b0`
- `0x437810`
- `0x4383d0`
- `0x468130`

But vtable presence is not enough to make a row part of the current steering path.

Current proof level:
- immediate state5 prep path only needs `0x443340 -> 0x443220 -> 0x465d70`
- later original consumption of connection `+0xa0` is proven at
  `0x4429b0 -> prep-object vtable +0x1c / 0x437810 -> +0x24 / 0x468130`
- `0x4430b0` and `0x4383d0` are still only family membership from the current evidence set

## Practical source consequence

- keep treating the connection `+0xa0` prep object as a real ctor/dtor-bounded class family
- do **not** source-own extra inherited rows just because they exist in this base/derived vtable
  family
- only pull those rows into source ownership when the concrete active caller/callee path is the
  current implementation target
