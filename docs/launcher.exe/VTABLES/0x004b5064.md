# 0x004b5064 - CLTLoginState_State5

## Overview
Family B / B1-branch helper for another custom slot 2 + slot 3 pair. Also an 8-byte cached-pointer object.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00439590` | specialized code | custom slot-2 override; uses `0x00434d00`, otherwise falls back to `0x00438df0` |
| 3 | `0x00439520` | specialized code | caches upstream object unless it reports `2` or `4`; then checks mediator state and posts event `0x10` on success |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x00439190` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000005`, returns false |
| 7 | `0x00438c60` | tiny state-id getter | undefined code in Ghidra; returns constant `5` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x004394f0` | constructor / initializer | installs vtable `0x004b5064` |

## Key specialized methods
- `0x00439590` shows the same inheritance-like pattern as `0x004393f0`: it handles a special case itself and otherwise delegates back to the shared `0x00438df0` implementation.
  - current tighter read of that special case:
    - it only takes the local branch when `LaunchPadClient_GetVtableOffset(workItem) == 0x0b`
    - it reads the payload/status dword through `0x434d00`
    - when that dword is non-zero it arms owner byte `+0x2d`
    - then it reads the cached upstream object's phase code and switches to that helper state
- `0x00439520` is the concrete behavior that differentiates this class: it caches an upstream object unless it reports `2` or `4`, probes mediator readiness via `0x004433c0`, and either switches state `2` or runs the `0x41b500` prep/send pair then posts event `0x10`.
  - current source now also owns the next helper shapes behind that branch without driving them live on
    the working path yet:
    - `0x41ce80` copy into connection `+0x98`
    - `0x41f30` raw-type-`1` prefix plus stored `0x136` block send through connection vtable `+0x24`
  - practical replacement restraint after runtime regression:
    - those helper shapes stay **dormant** in source until the full copied `0x136` auth-reply block is
      validated strongly enough to replace the current launcher-owned bootstrap continuation safely
- Practical code-4 relevance:
  - `0x441850` synthesizes a local type-`0x0b` work-item and routes it back through margin
    connection vtable `+0x10`
  - on the margin leaf that can fall through `0x44af60 -> 0x41afc0` and land here at slot 2
  - so this state5 slot-2 family is the next original continuation seam after the local type-`0x0b`
    work-item, not the slot-6 margin-reply body
- Slot 7 (`0x00438c60`) returns `5`.

## Composition notes
- This is another B1-derived state object built around a custom slot 2 override plus a simple slot 3 workflow method.
- The shared slot-6 failure stub `0x00439190` ties it structurally to `0x004b503c`.
