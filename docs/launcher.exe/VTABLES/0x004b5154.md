# 0x004b5154 - CLTLoginState_State11

## Overview
Family B state `0x0b` in the post-auth margin chain. Allocated as an 8-byte object with two small counters/flags.

Provisional better-name suggestion:
- `CLTLoginState_PostAuthMarginLoadPending`

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x0043c020` | specialized code | builds/sends a raw margin packet whose opcode byte is `0x4d`, then posts event `0x15` |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x00440320` | specialized code | handles opcode `0x10` / load-character reply fragments, then switches state `9` and posts event `0x16` when complete |
| 7 | `0x00438cb0` | tiny state-id getter | undefined code in Ghidra; returns constant `11` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x00439720` | constructor / initializer | installs vtable `0x004b5154` |

## Key specialized methods
- `0x0043c020` is the immediate post-auth send step. It gathers many mediator fields, serializes them into a raw margin packet with first byte `0x4d`, sends it, and posts event `0x15`.
- source ownership note:
  - the active scaffold now keeps that packet build/send shape in
    `CLTLoginState_State11::Slot3_BeginOrContinue`
  - the mediator side only retains the narrower current-margin-connection transport helper for
    `0x41af70`
  - source now also reifies the local packet-builder helper family used by `0x43c020`:
    - `0x439840 = CLTLoginMediatorPacketBuilderEnvelope_Initialize`
    - `0x43a470 = CLTLoginMediatorPacket0x4d_ResetAndInitialize`
    - `0x43a640 = CLTLoginMediatorPacket0x4d_SetRealFirstName`
    - `0x43a740 = CLTLoginMediatorPacket0x4d_SetRealLastName`
    - `0x43a840 = CLTLoginMediatorPacket0x4d_SetBackground`
    - `0x43a940 = CLTLoginMediatorPacket0x4d_SetGameSessionId`
- newer packet-debug detail from `0x43e540` now makes the consumed owner fields much less generic:
  - owner `+0x134..+0x174` = 17 appearance/customization ids:
    - `SkinToneID`
    - `BodyID`
    - `HeadID`
    - `HairID`
    - `HairColorID`
    - `TattooID`
    - `FacialHairID`
    - `FacialHairColorID`
    - `StartingHat`
    - `StartingGlasses`
    - `StartingShirt`
    - `StartingGloves`
    - `StartingCoat`
    - `StartingPants`
    - `StartingTights`
    - `StartingShoes`
    - `TraitID`
  - owner `+0x178` = `RealFirstName`
  - owner `+0x198` = `RealLastName`
  - owner `+0x1b8` = `Background`
  - final owner vtable `+0x148` string = `GameSessionID`
- that final string getter is now recovered as `0x41f320 = CLTLoginMediator_GetGameSessionId664`, a tiny getter returning owner `+0x664`
- concrete writer ownership for that shared trailing field is now better split:
  - `0x00421220 = LaunchPadClient_OnLoginRequestStatus` writes paired owner `+0x660`
    and owner `+0x94` first-string state
  - `0x00420ef0 = LaunchPadClient_OnPlayRequestStatus` is the direct writer to owner `+0x664`
- newer compare against state8 sender `0x0043bd20` narrows the shared-source question:
  - `GameSessionID` is the clearly shared trailing field between the two senders
  - owner `+0x178/+0x198/+0x1b8` are **not** consumed by `0x0043bd20`
  - broader mediator field-offset scan has not yet found another direct writer for
    `+0x178/+0x198/+0x1b8` beyond branch-specific `0x0041c3c0`
- `0x00440320` is the matching chunked reply path. It accumulates reply fragments in the owner's `+0xf1c` region and, once complete, switches state to `9` and posts event `0x16`.
- Slot 7 (`0x00438cb0`) returns `11`, which matches the post-auth state id reached from `0x004401a0`.

## Composition notes
- This is a concrete **post-auth login-state object** and an important intermediate stage in the later margin/loading pipeline.
- The chain from `0x004401a0` into this vtable is one of the strongest pieces of evidence for the larger `CLTLoginState_*` family structure.
