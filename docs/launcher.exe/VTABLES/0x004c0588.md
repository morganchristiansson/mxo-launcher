# VTable `0x004c0588` - owner-linked heap-buffer helper leaf on the `0x004c0540` base

## Current best read

This is a concrete owner-linked leaf in the `0x004c0540` refcounted helper cluster.

High-confidence ctor facts from `0x481770`:

- sets `+0x04 = 1` (non-interlocked refcount)
- allocates a heap buffer of caller-selected size into `+0x0c`
- zeros `+0x10`
- stores owner pointer at `+0x18`
- links itself through owner fields around `+0x288 / +0x294 / +0x290`

The exact family name is still unresolved, but it is clearly a richer owner-managed leaf than
`0x004c0564`.

## Recovered rows

Direct memory read of `0x004c0588`:

| Offset | Target | Current best read |
|---:|---:|---|
| `+0x00` | `0x0042f7e0` | shared plain `AddRef` helper |
| `+0x04` | `0x00481870` | owner-aware release / queue-link helper |
| `+0x08` | `0x004816f0` | shared getter over `+0x04` (`refcount04` here) |
| `+0x0c` | `0x00481750` | returns field `+0x0c` |
| `+0x10` | `0x00481750` | same getter reused again |
| `+0x14` | `0x00481760` | returns field `+0x10` |
| `+0x18` | `0x004818c0` | stores caller arg into field `+0x10` |
| `+0x1c` | `0x00437b40` | shared tiny `return 0` stub |
| `+0x20` | `0x00483080` | deleting dtor |

## Special `+0x04` behavior

`0x481870` is not the simple base-family release from `0x4816e0`.
Its current best bounded behavior is:

- if `refcount04 == 1`
- and owner `+0x18` is non-null
- and the owner-side pending count limit check passes

then it temporarily retains the object, links it into an owner-managed list/slot, and only then
performs the trailing decrement/delete test.

So this leaf is participating in an owner-managed pending/queued helper chain, not just raw heap
ownership.

## Destructor behavior

Deleting dtor `0x483080` runs concrete dtor `0x481810`, which:

- restores vtable `0x004c0588`
- unlinks/releases owner-managed state through owner field `+0x18` when present
- frees the owned heap buffer at `+0x0c`
- collapses back to base vtable `0x004c0540`

## Related docs

- `0x004c0540.md`
- `0x004c0564.md`
- `0x004c05ac.md`
