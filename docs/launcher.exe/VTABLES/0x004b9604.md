# 0x004b9604 = cleanup / subobject-teardown carrier (not yet promoted to Crypto++ class)

Status: low confidence for exact class identity.

## Current interpretation

`0x004b9604` shows up through teardown helpers like `0x444570` and `0x450df0`, and its current use is
much more consistent with cleanup / destruction scaffolding than with a meaningful semantic library class.

Reasons:

- decompile shows negative-offset access (`this[-3]`, `this[-1]`-style patterns)
- repeated vtable rewrites during destruction
- multiple frees of internal buffers / arrays
- appears in `0x447dd0` only as local cleanup state, not as the semantic pubkey.dat sink object

That combination strongly suggests one of:

- MI subobject teardown
- partially-constructed object cleanup
- compiler/runtime EH support object
- OOAnalyzer synthetic class boundary

## Why this is *not* currently treated as Crypto++

Unlike `0x004b77f8`, there is no strong direct evidence here such as:

- exact Crypto++ named parameter strings
- recognizable public Crypto++ constructor shape
- obvious semantic participation in the data path

So even though this object may sit near Crypto++-family code, the current evidence does **not** justify
naming it as a real Crypto++ class.

## Key related functions

| Address | Current role |
|---|---|
| `0x444570` | cleanup helper with negative-offset / MI smell |
| `0x450df0` | destructor / heap-owned member cleanup |
| `0x447dd0` | uses a local `0x4b9604` object only in teardown flow |

## Working rule

Keep `0x004b9604` documented as cleanup scaffolding unless a non-teardown semantic use appears.
Do not rename it into a Crypto++ class based only on proximity to Crypto++ code.
