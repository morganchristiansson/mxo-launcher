# 0x004b8380 - pooled read-side prefix-buffering sink used by `CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch`

## Current best read
This vtable now reads as a small pooled helper object allocated only on the read-helper path inside:
- `0x44d500 = CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch`

Construction/use shape from `0x44d500`:
- object size is `0x24` bytes (`0x44cf70` pool allocator)
- fresh object fields are initialized as:
  - `+0x00` = vtable `0x004b8380`
  - `+0x08 .. +0x11` = zeroed local prefix buffer
  - `+0x14` = `0x0a` remaining prefix-byte count
  - `+0x18` = downstream sink object pointer (the stack-local `0x004b84f0` message-ref sink)
  - `+0x1c` = embedded buffered-transformation helper initialized by `0x46cc70`
- that object is then passed as the attached output sink into:
  - `0x454310 = StreamTransformationFilter_ctor`
  - which is later consumed by `0x44bca0 = CPacketDecryptor_DecryptPacket`

So current best role is:
- **read-side prefix-buffering / stripping sink**
- it consumes the first 10 transformed bytes locally
- once that prefix is complete, it forwards later bytes into the real output message-ref sink

## Recovered behavior
### Slot `+0x14` / `0x44bbe0 = CStreamPacketEncryptionModuleReadPrefixSink_Put2`
High-value body recovered from direct decompilation/disassembly:
- if helper `+0x14` is still non-zero:
  - copies up to that many bytes into the local inline prefix buffer
  - decrements helper `+0x14`
  - when helper `+0x14` reaches zero, calls helper `+0x1c -> vtable +0x0c` with:
    - bytes at helper `+0x0c`
    - length `6`
- if input bytes still remain after that prefix stage:
  - forwards them into helper `+0x18 -> vtable +0x14`
  - also mirrors them into helper `+0x1c -> vtable +0x0c`

That is strong evidence this class is not a generic sink but a **prefix-buffering read helper** that peels a fixed 10-byte transformed header before forwarding the rest.

### Slot `+0x20` / `0x44bc80`
- if helper `+0x14` is still non-zero, returns false-ish immediately
- otherwise delegates into helper `+0x18 -> vtable +0x20`

This fits the same buffered-prefix role:
- downstream completion/flush-like behavior is blocked until the initial 10-byte prefix has been fully consumed

### Slot `+0x00` / `0x44d0c0 = CStreamPacketEncryptionModuleReadPrefixSink_dtor`
- tiny pooled destructor wrapper
- resets both the outer vtable and the embedded helper at `+0x1c` back to `0x004af29c`
- when delete flag bit 0 is set, returns the object to the freelist at `DAT_004f80b4`

## Relationship to neighboring tables
This table is tightly paired with:
- `0x004b84f0`
- `0x004b8438`

Current best composition on the read-helper path:
- `0x004b84f0` = actual output message-ref sink
- `0x004b8380` = prefix-buffering wrapper sitting in front of that sink

## VTable highlights
| Offset | Function | Current best read |
|---:|---:|---|
| `+0x00` | `0x44d0c0` | `CStreamPacketEncryptionModuleReadPrefixSink_dtor` | pooled dtor / freelist return |
| `+0x14` | `0x44bbe0` | `CStreamPacketEncryptionModuleReadPrefixSink_Put2` | prefix-buffering `Put2`-style body |
| `+0x20` | `0x44bc80` | `FUN_0044bc80` | delegate only after prefix is fully consumed |
