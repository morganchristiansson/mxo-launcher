# 0x004b01c8 CLTLoginMediator

New base-class clarification from the current destructor/raw-vtable pass:
- `0x004af2b8` now reads as the abstract base/interface directly above this concrete owner
- `0x41f810 = CLTLoginMediator_DeletingDestructor` calls `0x41f6a0 = CLTLoginMediator_Destructor`
- `0x41f6a0` performs owner cleanup, then retables `this` back to `0x004af2b8` before returning
- raw `0x004af2b8` is almost entirely `0x48bc34 = purecall` across its exposed slots through
  `+0x18c`, which is a strong fit for the source-side `ILTLoginMediator` abstraction already used
  by the replacement
- concrete `0x004b01c8` fills that surface and extends it with owner-only slot
  `+0x190 = 0x41f840 = CLTLoginMediator_AppendLateEntryStringTriple1470`

Focused startup-path note:
- vtable slot `0x004b01d0 -> 0x0041b160` (`CLTLoginMediator_Initialize`) now has a clearer current read:
  - parameter `networkEngineOverride`
  - if null, it falls back to global `0x4d6304`
  - it stores that engine pointer at mediator `+0x14`
- that aligns this initializer directly with the same `CLTThreadPerClientTCPEngine` object built at launcher helper `0x40a380`
- important wrapper/owner split clarification from the current `0x40a380` revisit:
  - launcher startup calls the **resolved arg6 wrapper** slot `+0x08` at `0x40a3fe`
  - do **not** collapse that wrapper call directly onto owner vtable `0x004b01c8 +0x0c`
  - current owner vtable `+0x0c` body is `0x41f510`, which now reads as reset/clear-owned-runtime-state logic rather than a simple engine-setter
  - practical consequence: startup ownership for the arg5 -> arg6 handoff belongs at `0x40a380`, while later arbitrary arg5 dispatch should not lazily perform mediator bind/reset side effects
- current source now mirrors the highest-confidence startup effects of owner `+0x08 / 0x41b160`
  directly on `CLTLoginMediator::Initialize(networkEngineOverride)`, reached from the resolved
  wrapper `+0x08` handoff; that keeps the wrapper/owner split explicit while still concentrating
  engine install, state0 install, active-source registration, auth-list refresh, and owner `+0x680`
  child allocation in one startup-owned place
- newer post-auth/state-8 review now also makes the following owner-side family much more concrete:
  - `+0x40 / 0x41f2e0` = current character-slot record table accessor over owner `+0x688`
  - `+0x44 / 0x41f300` = inline thunk using owner byte `+0xcc8` to read that same current character-slot entry
  - `+0x48 / 0x41f350` = bootstrap child opaque reply blob getter over owner `+0x680 + 0x108`
  - `+0x4c / 0x41f360` = sibling bootstrap child opaque reply blob getter over owner `+0x680 + 0x10c`
  - recovered helper `0x41b260` = state-8 margin-route string getter over owner `+0x818`
  - writer slot `+0x190 / 0x41f840` no longer belongs with `+0x818`; newer direct disassembly now shows it adds `0x1470` and forwards into `0x41f640 = StringTripleArray_Append`, so it writes the later late-entry string-triple vector exposed through arg6 `+0x118`
  - `+0xfc/+0x100/+0x104/+0x108` = world-descriptor readers over owner `+0xd84`
- broader writer `0x43f300 = CLTLoginState_AuthenticatePending_AuthMessageDispatch` now gives the key relation between them:
  - `+0xd84` is the world-descriptor table
  - `+0x688` is the character-slot table
  - `+0x818` is seeded by matching character world-id against the world-descriptor table and copying the descriptor name
  - tighter order from the current listing pass:
    - build/increment the `+0xd84/+0xd80` world-descriptor table first
    - normalize world status/type as each descriptor is stored
    - then call embedded helper reset `0x41d270`
    - cap `+0x684` to `<= 100` before the character loop
    - store each new `+0x688[index]` slot record before validating/joining it
    - then join `slotRecord.worldId` against `descriptor.worldId` and copy descriptor inline name into `+0x818[index]`
  - it also validates and normalizes payload enums on ingest:
    - world status valid range = `1..5`, else force `0`
    - world type valid range = `1..3`, else force `0`
    - character status valid range = `0..6`, else force `7`
  - negative result kept explicit: no direct `+0xcc8` rewrite is visible inside the one-time `0x43f300` table-rebuild body after the embedded reset sets it to `0xff`
- newer embedded-helper tightening from `0x41dba0 / 0x41d270 / 0x41dd00` shows those regions are not just loose adjacent fields:
  - owner `+0x684 .. +0xd7f` is one embedded helper/subobject (`CLTLoginMediatorSelectionRouteState_0x41dba0` in current Ghidra; current best descriptive read = mediator selection-route state)
  - its ctor `0x41dba0` initializes:
    - `+0x684` slot count
    - `+0x688[100]` slot-record pointer table
    - `+0x818[100]` route-string triples
    - `+0xcc8 = 0xff`
    - `+0xcd0 .. +0xd7f = 0`
  - reset `0x41d270` releases active `+0x688` slot records, clears active `+0x818` strings, then sets `+0x684 = 0` and `+0xcc8 = 0xff`
  - destroy `0x41dd00` calls that reset and then frees all 100 route-string buffers
  - practical consequence: current source should treat `+0x684/+0x688/+0x818/+0xcc8/+0xcd0..+0xd7f` as one ownership island and now models that region as the nested `CLTLoginMediatorSelectionRouteState` class
- together these now look like a real post-auth character/world/route/selection-state family rather than unrelated tiny helpers
- raw-vtable clarification from the current launcher page-`6` submit pass:
  - raw memory at `0x004b01c8` shows `0x41ecd0` stored at `0x004b01f8`
  - the launcher rich-edit submit helper `0x408400` calls resolved mediator slot `+0x30`
  - practical consequence: that launcher helper already reaches this same recovered
    `CLTLoginMediator::ProcessLoginRequest` boundary through the resolved
    `ILTLoginMediator.Default`-style surface
  - note: some older source/doc shorthand around `ProcessLoginRequest` collapsed nearby tiny
    getters/setters differently, so preserve raw launcher call displacements separately when they
    come from decompile
- bounded `2026-03-21` owner `+0xf18` provenance pass outcome:
  - within the bounded mediator/state9 static set, only:
    - `0x41ee60` zero-initializes owner `+0xf18`
    - `0x41e690` reads/seeds owner `+0xf18` into the state9 callback blob
  - runtime bracketing then proved a non-init writer must exist earlier:
    - still `0` at `0x41ecd0`, `0x41c1f0`, and state8 slot3 `0x43bd20`
    - already non-zero by state8 slot6 entry `0x43f930`
  - the concrete non-init writer is now isolated as:
    - `0x00440780 = CLTLoginState_State6_Slot6_HandleMarginOpcode7Or9Reply`
    - on opcode-`9` success it writes owner byte `+0xf14 = 1` and owner dword `+0xf18 = parsedReply(+0x09)`
  - canonical field naming and helper-split detail live under:
    - `VTABLES/0x004b508c.md`
  - short mediator-side consequence:
    - parsed `+0x09` is now best read as the opcode-`9` `UDPSessionSecret` / session-id dword
    - that field feeds owner `+0xf18`
    - the next helper-state target is chosen separately from cached upstream object `this+4` via vtable `+0x18`
  - direct displacement searches stayed deceptively weak because the real writer reaches `+0xf18` via base `+0xf14` plus `[eax+4]`, not a literal `[base+0xf18]`

| Offset | Function Address | Function Name | Instructions | Complexity | Calls |
|---------|------------------|---------------|--------------|------------|-------|
| +0x00 | 0x0041f020 | CLTLoginMediator_GetName | 6 | 1 | 0 | tiny literal getter returning the string `"CLTLoginMediator"`; this is the real first MSVC2003 vtable row on the original binary |
| +0x04 | 0x0041f810 | CLTLoginMediator_DeletingDestructor | 14 | 4 | 2 |
| +0x08 | 0x0041b160 | CLTLoginMediator_Initialize | 56 | 10 | 7 |
| +0x0c | 0x0041f510 | CLTLoginMediator_ResetOwnedRuntimeState | 75 | 14 | 11 | checks owner active byte `+0x04`, clears late-entry/vector/auth-bootstrap/session-helper/runtime child state, releases owner `+0x18/+0x1c/+0x680/+0x65c`, and clears `g_CurrentLoginMediator`; do not confuse this owner slot with the resolved arg6 wrapper `+0x08` call reached from launcher `0x40a380`; current source teardown now mirrors the highest-confidence reset subset through wrapper clear + `ResetLauncherConnectionsScaffold`/late-entry/session-helper cleanup and now also drops the source-owned `+0x680` child until the next startup/auth-path allocation |
| +0x10 | 0x0041f030 | CLTLoginMediator_IsReady | 4 | 1 | 0 | tiny getter returning owner byte `+0x04`; current best read for the concrete owner-side implementation of the wrapper-facing readiness check |
| +0x14 | 0x0041f040 | CLTLoginMediator_SetUnknownByte05True | 5 | 1 | 0 | tiny setter writing `1` to owner byte `+0x05`; concrete semantics still unresolved |
| +0x18 | 0x0041f050 | CLTLoginMediator_GetUnknownByte05 | 4 | 1 | 0 | tiny getter returning owner byte `+0x05`; concrete semantics still unresolved |
| +0x1c | 0x0041f060 | CLTLoginMediator_SetNoPatchLauncherVersionValue08 | 7 | 1 | 0 | tiny setter writing caller dword into owner `+0x08`; explicit nopatch path feeds this from parsed `"0.1"` / launcher-version resource state |
| +0x20 | 0x0041f070 | CLTLoginMediator_GetNoPatchLauncherVersionValuePtr08 | 4 | 1 | 0 | tiny getter returning `&owner+0x08`; paired with the launcher-version setter at `0x41f060` |
| +0x24 | 0x0041f080 | CLTLoginMediator_SetNoPatchClientVersionValue0c | 7 | 1 | 0 | tiny setter writing caller dword into owner `+0x0c`; explicit nopatch path feeds this from parsed `"0.1"` / client.dll version-resource state |
| +0x28 | 0x0041f090 | CLTLoginMediator_GetNoPatchClientVersionValuePtr0c | 4 | 1 | 0 | tiny getter returning `&owner+0x0c`; paired with the client-version setter at `0x41f080` |
| +0x2c | 0x0041aee0 | CLTLoginMediator_IsConnected | 27 | 3 | 1 | current tiny body checks owner current-helper pointer `+0x10`, then calls that helper/state vtable `+0x20`; returns `1` when the helper says connected/ready, else `0` |
| +0x30 | 0x0041ecd0 | ProcessLoginRequest | 109 | 14 | 8 | live original password-submit branch; current source-owned mirror now lives in `matrixstaging/game/src/libltclientlogin/loginmediator_auth_entry.cpp`, installs source state0 as the initial helper convention, copies owner `+0x94`, and keeps explicit default-off scaffolds for the later `g_LaunchPadGateState16State18 != 0` / state16/state18 family while the active happy path stays on the observed `g_LaunchPadGateState16State18 == 0` branch and hands off `state0 -> state2` before the owner `+0x680` bootstrap child; launcher-side auto-login should now prefer entering through this slot with an input block built from the configured auth strings instead of bypassing it with a direct state2 jump, and the later `0x448050` feeder should read that owner `+0x94` copy rather than a separate replacement-only credential stash; newer launcher-flow clarification: original `-user` / `-pwd` act like launcher-login prefill/auto-submit for this same branch, not a separate bypass mode; current launcher startup also uses this as the pre-client stdin-prompt submit API, and the active launcher path no longer keeps a synthetic post-InitClientDLL auth restart |
| +0x34 | 0x0041c0d0 | CLTLoginMediator_RequestAuthCloseAndSwitchToState0 | - | - | callback-side auth-close/state-reset helper: calls `+0x164`, then swaps owner `+0x10` back to global state0 object `g_LoginHelperState0` (`0x4f7868`); current page-6 rich-edit observer failure path reaches this through sibling `0x4091d0` |
| +0x38 | 0x0041f0a0 | CLTLoginMediator_GetProfileRootName | - | - | exact tiny body is only `lea eax,[ecx+0x94] ; ret`; wrapper-facing `Profiles\%s\...` callers therefore read the first inline string of owner `+0x94` directly |
| +0x3c | 0x0041f2d0 | CLTLoginMediator_GetDefaultSelectionIndex | - | - | exact tiny body is only `mov al,[ecx+0xcc8] ; ret`; practical correction: this is the current owner selection/slot byte, not a free-standing configured world id |
| +0x40 | 0x0041f2e0 | CLTLoginMediator_GetSlotRecordByIndex | 12 | 1 | 0 | current best read = tiny accessor over the embedded selection-route helper table at owner `+0x688`; returns `0` when `slotIndex == 0xff` |
| +0x44 | 0x0041f300 | CLTLoginMediator_GetCurrentSlotRecord | - | - | exact inline thunk: loads the same embedded selection-route helper byte at owner `+0xcc8`, then forwards to `0x0041f2e0`; do not confuse this owner-vtable row with the separate wrapper-facing `ILTLoginMediator.Default +0x44` current-slot-record object family |
| +0x48 | 0x0041f350 | CLTLoginMediator_GetBootstrapOpaqueReplyBlob108 | - | - | tiny getter returning bootstrap child field `owner +0x680 + 0x108`; direct body is only `mov eax,[ecx+0x680] ; mov eax,[eax+0x108] ; ret`; older startup/profile-string naming here was stale bleed-over from the wrapper-facing arg6 `+0x48/+0x4c` family |
| +0x4c | 0x0041f360 | CLTLoginMediator_GetBootstrapOpaqueReplyBlob10c | - | - | sibling tiny getter returning bootstrap child field `owner +0x680 + 0x10c`; same note: this owner-side row is not the wrapper-facing `ILTLoginMediator.Default +0x4c` string getter |
| +0x50 | 0x0041f370 | CLTLoginMediator_BootstrapRaw08AuxHandle50 | - | - | returns `owner +0x680 -> +0xf4 -> +0xa8` when present; see `startup_objects/0x4f78b8_AUTHBOOTSTRAP_CHILD_PLUS680.md` for the full child lifecycle |
| +0x54 | 0x0041f0b0 | CLTLoginMediator_HasBootstrapRaw08AuxHandle54 | - | - | bool-style wrapper over mediator vtable `+0x50` |
| +0x58 | 0x0041f390 | CLTLoginMediator_GetCrashReporterPromptForSecurId58 | - | - | returns owner `+0x680 + 0x104` byte; launcher/client wrapper-facing consumers use that low byte as crashreporter `PromptForSecurId` |
| +0x5c | 0x0041f3a0 | CLTLoginMediator_GetBootstrapField85OrFallback5c | - | - | returns `owner +0x680 -> +0xf4 + 0x85` when present, else static fallback; full ownership/lifecycle now lives in `startup_objects/0x4f78b8_AUTHBOOTSTRAP_CHILD_PLUS680.md` |
| +0x60 | 0x0041f3c0 | CLTLoginMediator_GetBootstrapStringF8Begin60 | - | - | returns the first dword / begin pointer of bootstrap child `+0x680 + 0xf8`; see the same child doc for ctor/layout detail |
| +0x64 | 0x0041f2b0 | CLTLoginMediator_GetBootstrapSuccessHeaderDword110 | - | - | tiny getter returning bootstrap child field `owner +0x680 + 0x110`; current best read from the state2 success-side child layout is the copied auth-reply success-header dword at raw reply offset `+0x07` |
| +0x68 | 0x0041f0c0 | CLTLoginMediator_HasLiveHlCfg68 | - | - | tiny getter returning owner byte `+0x140e`; exact client consumer is `client.dll:0x62198670` / `hl.cfg` gate at `arg6 +0x68` |
| +0x6c | 0x0041f0d0 | CLTLoginMediator_HasLiveAnCfg6c | - | - | tiny getter returning owner byte `+0x1416`; exact client consumer is `client.dll:0x62198770` / `an.cfg` gate at `arg6 +0x6c` |
| +0x70 | 0x0041f0e0 | CLTLoginMediator_HasLivePiCfg70 | - | - | tiny getter returning owner byte `+0x141e`; exact client consumer is `client.dll:0x62198870` / `pi.cfg` gate at `arg6 +0x70` |
| +0x74 | 0x0041f0f0 | CLTLoginMediator_HasLiveAiCfg74 | - | - | tiny getter returning owner byte `+0x1426`; exact client consumer is `client.dll:0x62198970` / `ai.cfg` gate at `arg6 +0x74` |
| +0x78 | 0x0041f100 | CLTLoginMediator_HasLiveCsCfg78 | - | - | tiny getter returning owner byte `+0x142e`; exact client consumer now pinned as `client.dll:0x62198a70` / `cs.cfg` gate at `arg6 +0x78` |
| +0x7c | 0x0041f110 | CLTLoginMediator_HasLiveBlCfg7c | - | - | tiny getter returning owner byte `+0x13fe`; exact client consumer now pinned as `client.dll:0x62198b70` / `bl.cfg` gate at `arg6 +0x7c` |
| +0x80 | 0x0041f120 | CLTLoginMediator_HasLiveIlCfg80 | - | - | tiny getter returning owner byte `+0x1406`; exact client consumer now pinned as `client.dll:0x62198c60` / `il.cfg` gate at `arg6 +0x80` |
| +0x84 | 0x0041f130 | CLTLoginMediator_HasLiveRlCfg84 | - | - | tiny getter returning owner byte `+0x1448`; exact client consumer now pinned as `client.dll:0x62198d50` / `rl.cfg` gate at `arg6 +0x84` |
| +0x88 | 0x0041f140 | CLTLoginMediator_HasLiveClCfg88 | - | - | tiny getter returning owner byte `+0x1452`; exact client consumer now pinned as `client.dll:0x62198e50` / `cl.cfg` gate at `arg6 +0x88` |
| +0x8c | 0x0041f150 | CLTLoginMediator_HasState8PersistenceData8c | - | - | live original `client.dll:0x62198fa0` calls this first as the mediator-backed `mcd.cfg` gate; corrected launcher proof shows it returns owner byte `+0x13f6`, not `+0x1452` |
| +0x90 | 0x0041f160 | CLTLoginMediator_HasLiveCuiCfg90 | - | - | tiny getter returning owner byte `+0x145a`; exact client consumer now pinned as `client.dll:0x621993d0` / `cui.cfg` gate at `arg6 +0x90`; current replacement still keeps the later on-disk `cui.cfg` save mismatch explicit |
| +0x94 | 0x0041ad80 | CLTLoginMediator_GetLiveHlCfg94 | 10 | 2 | 0 | returns owner `+0x1408` and writes out-length `+0x140c`; exact client consumer is `hl.cfg` getter at `arg6 +0x94` |
| +0x98 | 0x0041ada0 | CLTLoginMediator_GetLiveAnCfg98 | 10 | 2 | returns owner `+0x1410` and writes out-length `+0x1414`; exact client consumer now pinned as `an.cfg` getter at `arg6 +0x98` |
| +0x9c | 0x0041adc0 | CLTLoginMediator_GetLivePiCfg9c | 10 | 2 | returns owner `+0x1418` and writes out-length `+0x141c`; exact client consumer now pinned as `pi.cfg` getter at `arg6 +0x9c` |
| +0xa0 | 0x0041ade0 | CLTLoginMediator_GetLiveAiCfgA0 | 10 | 2 | returns owner `+0x1420` and writes out-length `+0x1424`; exact client consumer now pinned as `ai.cfg` getter at `arg6 +0xa0` |
| +0xa4 | 0x0041ae00 | CLTLoginMediator_GetLiveCsCfgA4 | 10 | 2 | returns owner `+0x1428` and writes out-length `+0x142c`; exact client consumer now pinned as `cs.cfg` getter at `arg6 +0xa4` |
| +0xa8 | 0x0041ae40 | CLTLoginMediator_GetLiveBlCfgA8 | 10 | 2 | returns owner `+0x13f8` and writes out-length `+0x13fc`; exact client consumer now pinned as `bl.cfg` getter at `arg6 +0xa8` |
| +0xac | 0x0041ae60 | CLTLoginMediator_GetLiveIlCfgAc | 10 | 2 | returns owner `+0x1400` and writes out-length `+0x1404`; exact client consumer now pinned as `il.cfg` getter at `arg6 +0xac` |
| +0xb0 | 0x0041ae80 | CLTLoginMediator_GetLiveRlCfgB0 | 10 | 2 | returns owner `+0x1440` and writes out-length `+0x1444`; exact client consumer now pinned as `rl.cfg` getter at `arg6 +0xb0` |
| +0xb4 | 0x0041aea0 | CLTLoginMediator_GetLiveClCfgB4 | 10 | 2 | returns owner `+0x144c` and writes out-length `+0x1450`; exact client consumer now pinned as `cl.cfg` getter at `arg6 +0xb4` |
| +0xb8 | 0x0041ae20 | CLTLoginMediator_GetLiveCuiCfgB8 | 10 | 2 | returns owner `+0x1454` and writes out-length `+0x1458`; exact client consumer now pinned as `cui.cfg` getter at `arg6 +0xb8`; current replacement still keeps the later on-disk `cui.cfg` save mismatch explicit |
| +0xbc | 0x0041f170 | CLTLoginMediator_GetState8PersistenceHeaderF48 | - | - | live original `client.dll:0x62198fa0` copies `0x20` bytes from this pointer into `DAT_629ea67c`; launcher getter returns owner `+0xf48` |
| +0xc0 | 0x0041f180 | CLTLoginMediator_GetState8PersistenceBodyF88 | - | - | live original `client.dll:0x62198fa0` copies `0x465` bytes from this pointer into its mediator-backed `mcd.cfg` body state; launcher getter returns owner `+0xf88` |
| +0xc4 | 0x0041aec0 | CLTLoginMediator_GetState8PersistenceOverflow13f0 | 10 | 2 | 0 | live original `client.dll:0x62198fa0` asks for pointer+out-length here; returns owner `+0x13f0` and stores length from `+0x13f4` when caller supplies the out pointer |
| +0xc8 | 0x0041f190 | CLTLoginMediator_HasState8Section11Dword145c | - | - | newer bounded state8 `mcd.cfg` sibling getter; returns true when owner dword `+0x145c != 0` |
| +0xcc | 0x0041f1a0 | CLTLoginMediator_GetState8Section11Dword145c | - | - | returns owner dword `+0x145c` |
| +0xd0 | 0x0041f1b0 | CLTLoginMediator_GetState8Section11String1460 | - | - | returns owner `+0x1460`; current best read = small-string-like companion to the same state8 section-`0x0b` side effect; replacement wrapper minimization now keeps the returned small-string-like scratch on `CLTLoginMediator` instead of wrapper globals |
| +0xd4 | 0x0041b4f0 | CLTLoginMediator_GetMarginConnectionField85D4 | - | - | tiny getter returning owner `+0x1c + 0x85`; direct assembly is only `mov eax,[ecx+0x1c] ; add eax,0x85 ; ret`, so the original slot is the live margin-connection seed pointer itself, not a fallback chooser; current replacement now mirrors the already-recovered CERT_Challenge Twofish key into that live connection field earlier on the active path because bounded smoke showed direct client `+0xd4` callers could arrive before the narrowed code-5 seam populated it; keep launcher-owned sidecar reuse only as bounded fallback/diagnostic glue on `+0xd4` until runtime is stable enough to prune it without risking game entry |
| +0xd8 | 0x0041af00 | CLTLoginMediator_GetArg7SelectionUpperBoundExclusive | - | - | exact state-gated byte getter: if current helper/state code `< 3`, returns `0`; otherwise returns owner byte `+0x684` (the active slot-record / selection-entry count) |
| +0xdc | 0x0041b220 | CLTLoginMediator_MapSelectionName | - | - | exact slot body is the same state-gated slot-record heap-string reader: if state code `>= 3`, index `< 100`, and owner `+0x688[index]` exists, returns that slot's heap-string pointer at `+0x14`; otherwise returns `0` |
| +0xe0 | 0x0041b260 | CLTLoginMediator_GetVariantWorldName | - | - | exact state-gated owner string reader over `+0x818[index*3]`: if current helper/state code `< 3`, index `>= 100`, or begin==end, returns `0`; otherwise returns the copied descriptor inline-name pointer stored under owner `+0x818` |
| +0xe4 | 0x0041b2a0 | CLTLoginMediator_GetSlotRecordStatusByIndex | - | - | returns slot-record payload byte `+0x0b` from owner `+0x688[index]`; this now materially tightens the launcher page-`7` row high word too, because the same selected-row high word feeds this consumer in `0x40d530/0x40d6f0`, making that high word better fit the active selection-entry / slot-record index; default/fallback result is `7` |
| +0xe8 | 0x0041ec00 | CLTLoginMediator_RemoveSlotRecordAndCompactRouteStateByIndex | - | - | current tighter decompile-backed read from `0x40ec70` success-side delete-character recovery: when state code `> 2`, input `< 100`, and owner slot count `+0x684 != 0`, it decrements that count **before** touching `+0x688[index]`, releases/removes slot object `+0x688[index]`, shifts later `+0x688` pointers and `+0x818` string-triples down, then clears the final pointer/string tail; negative result: no direct `+0xcc8` rewrite is visible in this body; current launcher-side callsite passes the selected row item-data high word (current tighter auth-valid read: active selection-entry / slot-record index) here after `WaitForEvent(8)` returns `0`, immediately after deleting `Profiles\%s\%s` and before rebuilding the list |
| +0xec | 0x0041c1f0 | CLTLoginMediator_PersistSelectionContextAndSwitchToState8 | - | - | active state-3 wait branch: checks current state code `> 2`, writes the selected slot byte to `+0xcc8`, then copies the remaining contiguous `0xb0`-byte selection/config body from the caller's `0xb4` handoff into owner `+0xcd0..+0xd7f` before switching helper state to `8`; keep this ownership on the mediator/owner side rather than inventing a state3-local slot-3 body; newer `0x41c1f0 + 0x41b450 + 0x43bd20` tightening now matters for the existing-character continuation too: `0x41c1f0` itself only calls `0x41b450(8)`, but that helper immediately re-enters the new helper slot-3 with the old helper object, so the active path does **not** stop at plain `currentState=state8` — it immediately runs state8 slot3, which then hands off into helper4/state4 while margin connection state is still not ready; practical consequence: the later type-2 margin connect-status completion is expected to land on state4 slot2, not to be meaningfully consumed by shared state8 slot2; newer `0x41c1f0 + 0x43bd20` tightening also makes the packet relation concrete: `0x41c1f0` copies the 11 dword-blocks monotonically into owner `+0xcd0..+0xd7f`, while state8 slot3 later emits them in packet order `0x09/0x19/0x29`, `0x79/0x89/0x99/0xa9`, then `0x39/0x49/0x59/0x69`; negative result: state8 fixed packet GCID dwords `0x01/0x05` still come from the current slot-record getter `+0x44`, not from persisted snapshot block `+0xcd0[0..1]`; launcher-side tightening now closes the upstream bridge more narrowly too: the selection page resolves through `0x40d6f0`, which writes `CLauncher+0xa8/+0xac` and persists `Last_WorldName`, while the direct success-side `+0xec` call is then best read from `client.dll:0x62170f48 = InitClientDLL_BeginLoadingCharacterFlow`, where the client zero-initializes and fills the stack-local `0xb4` handoff immediately before calling this slot; newer layout tightening from that client builder now gives concrete names to the later persisted blocks too: input `+0x24/+0x34/+0x44/+0x54/+0x64/+0x74/+0x84/+0x94/+0xa4` line up with `il.cfg/hl.cfg/an.cfg/rl.cfg/cl.cfg/pi.cfg/ai.cfg/(cs then bl shared temp)/cui.cfg`; tighter direct-producer evidence from `0x6211d3e0 + 0x62170e2a..0x62170f48` also closes the old block04 question further: the client first zero-initializes the whole `0xb4` handoff, then only proves writes at `+0x00` and `+0x24..+0xa4`, so both `block04` (`+0x04..+0x13`) and `block14` (`+0x14..+0x23`) are currently better treated as zero on the proven direct success-side path; current no-GUI launcher bridge no longer claims a pre-client direct call here; it now mirrors launcher page-`7` command-`8` writeback into `CLauncher+0xa8/+0xac` plus `Last_WorldName`, then leaves the direct `+0xec` commit on the proven client-side producer path while still keeping recovered slot/world/descriptor data only as separate shadow evidence; negative result from the concrete launcher `+0xf0` caller `0x40ec70`: that state7/event-8 corridor is delete-character/removal oriented, so it is **not** the success-side `+0xec` producer |
| +0xf0 | 0x0041c390 | CLTLoginMediator_SetSelectionIndexAndSwitchToState7 | - | - | sibling owner-side selection-context setter: when current state code `> 2` and input `< 100`, stores the byte at `+0xcc8` and switches helper state to `7`; current no-GUI launcher bridge no longer claims this narrower owner-side pre-commit on the pre-client selection-success path; newer launcher-side tightening from `0x40ec70` shows one concrete caller uses this in a delete-character/event-8 corridor rather than the still-hidden success-side `+0xec` producer |
| +0xf4 | 0x0041f1c0 | CLTLoginMediator_GetState8PersistenceF1c | - | - | bounded `mcd.cfg` / character-info getter; tiny body returns owner `+0xf1c`; current Ghidra type tightening now names that returned block `CLTLoginMediatorCharacterPersistenceData_0x41d900`, the same shared owner layout also reused earlier at owner `+0x108` on the helper11 create/load-character corridor; live consumer family reads base text there and later `+0x70/+0x90/+0xb0`, while the real producer is earlier state8/load-character reply `0x43f930` |
| +0xf8 | 0x0041af30 | CLTLoginMediator_GetWorldDescriptorCountD80 | - | - | tiny owner reader returning byte `+0xd80` when current state code `> 2`; current best practical read is the live world-descriptor count used by the launcher selection-world list path (`0x40e5b0`) |
| +0xfc | 0x0041b2e0 | CLTLoginMediator_GetDescriptorInlineNameByIndex | - | - | returns world-descriptor inline name `payload + 0x03` from owner `+0xd84[index]` |
| +0x100 | 0x0041b320 | CLTLoginMediator_GetDescriptorStatusByIndex | - | - | disassembly now confirms this reads world-descriptor status byte `payload + 0x17` from owner `+0xd84[index]`; keep wrapper/source split explicit because the earlier startup arg7 path still uses a synthetic gate byte for the same wrapper slot before the owner table exists |
| +0x104 | 0x0041b360 | CLTLoginMediator_GetDescriptorTypeByIndex | - | - | disassembly now confirms this reads world-descriptor type byte `payload + 0x18` from owner `+0xd84[index]` |
| +0x108 | 0x0041b3a0 | CLTLoginMediator_GetDescriptorPopulationNibbleByIndex | - | - | returns low nibble of world-descriptor population byte `payload + 0x1f` when in `1..3` |
| +0x10c | 0x0041f2c0 | CLTLoginMediator_GetRouteDescriptor30 | - | - | tiny getter returning owner `+0x30`; event-`0x18` observer callback consumes the first two dwords there as a small-string begin/current pair; replacement wrapper minimization now keeps that wrapper-facing 3-dword object on `CLTLoginMediator`, now source-grouped with the related route/descriptor getters in `matrixstaging/game/src/libltclientlogin/loginmediator_margin_route.cpp`, and leaves `src/launcher_mediator_abi.cpp` as a thin forwarder |
| +0x110 | 0x0041c110 | No Name found | - | - | switches to helper `0x4f78a0`; writes helper-local byte `+1` first |
| +0x114 | - | No Name found | - | - | - |
| +0x118 | 0x0041af50 | CLTLoginMediator_GetLateEntryList1470 | - | - | tiny getter returning owner `+0x1470`; newer `0x41f840 -> 0x41f640` tightening now shows that container holds 12-byte string-triple entries, not generic opaque triples; event-`0x18` callback still only needs the outer vector-like begin/end/capacity shape, but later client consumer `0x62017150` reads the first dword of each entry as a filename-like string and maps it through `ClientMasterDatabase_MapFilenameToMetricId` / `METR` metadata; replacement source now mirrors owner `+0x1470/+0x1474/+0x1478` directly as the raw vector header of deep-copied string-triple entries, and now keeps this getter itself as the same direct owner return before the later event-`0x18` read |
| +0x11c | 0x0041c1a0 | No Name found | - | - | writes owner `+0x104`, then switches helper state to `0x4f7880` when current state code > 2 |
| +0x120 | 0x0041c3c0 | CLTLoginMediator_ProcessCreateCharacterInput120 | - | - | recovered create-character source-block writer: writes owner `+0x12c` (selected world-descriptor index / selector because `0x41c3c0` bounds-checks it against owner vtable `+0xf8` and `0x4401a0` uses it to index owner `+0xd84`), `+0x134..+0x177`, `+0x108`, `+0x178`, `+0x198`, `+0x1b8`, then calls `0x41b450(10)`; practical consequence for the replacement: do not stop at plain `currentState=state10`, because the active continuation needs the immediate state10 slot-3 claim-name send (`0x43bf90`) |
| +0x124 | 0x0041f1d0 | CLTLoginMediator_SetState9CallbackObjectTriple84_88_8c | - | - | owner-side mirror for the wrapper-facing `ProvideStartupTriple` capture; stores `+0x84/+0x88/+0x8c` |
| +0x128 | 0x0041f200 | CLTLoginMediator_GetOwnerCallback84 | - | - | tiny getter returning owner field `+0x84`; current best bounded read is the startup-triple callback / netShell object captured by `+0x124` |
| +0x12c | 0x0041f210 | CLTLoginMediator_GetOwnerObject8c | - | - | tiny getter returning owner field `+0x8c`; current best bounded read is the third startup-triple object captured by `+0x124` |
| +0x130 | 0x0041f310 | CLTLoginMediator_GetSessionCallbackHelper65c | - | - | tiny getter returning owner `+0x65c`; the later alternate state18/helper path uses that helper's string `+0x18` to feed owner `+0x664` (`GameSessionID`) |
| +0x134 | 0x00420d00 | CLTLoginMediator_EnsureSessionCallbackHelper65c | - | - | lazy allocator for owner `+0x65c`; builds a `0x30`-byte helper through `0x420ca0` when null |
| +0x138 | 0x00420360 | CLTLoginMediator_SwitchToState18IfLaunchPadGateState16State18Set | - | - | tiny branch on global `g_LaunchPadGateState16State18` (`0x4d66ec`); when set, switches helper state to `18`; keep this separate from the active state2 -> owner `+0x680` bootstrap-child handoff |
| +0x13c | 0x004202c0 | CLTLoginMediator_HelperSlot13c_InvokeSessionHelperVtable4 | - | - | if owner `+0x65c` exists, jumps through helper vtable `+0x04`; otherwise returns; replacement wrapper now forwards directly into a `CLTLoginMediator` method that owns the session-helper pump/logging |
| +0x140 | 0x00420380 | CLTLoginMediator_HelperSlot140_CreateStationLoginSideEffect | - | - | touches literal `station_login`, mirrors result into owner byte `+0x670`, and conditionally calls `ShellExecuteA`-style external path; earlier startup `0x406470` reaches this when `g_LaunchPadGateState16State18` is non-zero |
| +0x144 | 0x004202e0 | CLTLoginMediator_ClearStationLoginSideEffect | - | - | login-request-success prelude: clears owner byte `+0x670` and closes literal `station_login` side-effect when that flag is set |
| +0x148 | 0x0041f320 | CLTLoginMediator_GetGameSessionId664 | - | - | tiny getter returning owner `+0x664`; state-8/state-11 packet debug printers identify that string as `GameSessionID`; concrete writers now in scope are later launchpad/play paths like `0x420ef0` and `0x421a50 -> 0x420e70` |
| +0x14c | 0x0041f330 | CLTLoginMediator_SetSharedMarginPacketField660 | - | - | tiny setter writing owner dword `+0x660`; login-request success `0x421220` pushes stack arg `[ebp+0x14]` here before later `+0x664` writers run |
| +0x150 | 0x0041f270 | ProcessCallbackData | - | - | direct writer for the owner `+0x94` bootstrap/source first string; `0x421220` success pushes `[ebp+0x10]` here, so do **not** confuse it with the shared `GameSessionID` string at `+0x664` |
| +0x154 | 0x0041f340 | CLTLoginMediator_GetSharedMarginPacketField660 | - | - | tiny getter returning owner dword `+0x660`; current paired writer is `+0x14c / 0x41f330` |
| +0x158 | 0x0041c510 | CLTLoginMediator_SetState9OptionalField90AndSwitchToState13 | - | - | state-gated helper: when current state id is `12`, writes owner dword `+0x90` from its arg then switches helper state to `13`; not a `RealFirstName/RealLastName/Background/GameSessionID` writer |
| +0x15c | 0x0041f230 | CLTLoginMediator_HasMarginConnection1c | - | - | tiny bool getter returning whether owner margin connection field `+0x1c` is non-null |
| +0x160 | 0x0041c590 | CLTLoginMediator_ForwardThroughReadyMarginConnection24 | - | - | connection forwarder: if owner margin connection `+0x1c` exists and its state dword `+0x34 == 2`, forwards the arg through connection virtual `+0x24`; not a shared packet-field writer |
| +0x164 | 0x0041b3f0 | CLTLoginMediator_RequestAuthConnectionCloseWaitEvent1 | - | - | tiny auth-close / shared-slot1-event-`1` arm: if owner auth connection `+0x18` exists, sets owner byte `+0x2c`, and when connection state `+0x34` is `1/2` calls its vtable `+0x0c(1)`; launcher teardown `0x40b360` only waits for event `1` when this returns true |
| +0x168 | 0x0041f220 | CLTLoginMediator_GetAuthConnection18 | - | - | tiny getter returning owner auth connection pointer `+0x18` |
| +0x16c | 0x0041b420 | CLTLoginMediator_HandleState9Opcode11SuccessSideEffect | - | - | owner-side state9-success side effect: clears owner byte `+0xf14`, sets owner byte `+0x2d`, and when margin connection `+0x1c` is in state `1/2` calls its vtable `+0x0c(1)`; launcher teardown also reuses this same slot as the wrapper-facing close/wait-event-`0x0f` predicate before `WaitForEvent(0x0f)` |
| +0x170 | 0x0041ddb0 | CLTLoginMediator_RegisterLoginObserver | - | - | inserts an observer/listener object into owner `+0x674`; client `WaitForEvent` and later runtime observer paths both use this slot through resolved arg6 `+0x170`; current best container home is `startup_objects/0x4f78b8_OBSERVER_TREE_PLUS674.md` |
| +0x174 | 0x0041dde0 | CLTLoginMediator_UnregisterLoginObserver | - | - | removes an observer/listener object from owner `+0x674`; client `WaitForEvent` uses this through resolved arg6 `+0x174`; current best container home is `startup_objects/0x4f78b8_OBSERVER_TREE_PLUS674.md` |
| +0x178 | 0x0041f240 | CLTLoginMediator_GetLastStatus80 | - | - | tiny getter returning owner `+0x80`; client observer/error handlers use this through resolved arg6 `+0x178` |
| +0x17c | 0x0041af80 | CLTLoginMediator_HandleAuthConnectionCompletionFallback | - | - | auth-connection completion fallback used from auth leaf `0x449a70`: filters to owner `+0x18`, clears owner `+0x18` on work type `1`, then re-enters current helper raw vtable entry `+0x00` (slot 1). Practical split there: type-`2` auth connect status lands on state1 slot 1 / `0x4390b0`, while non-type-`2` auth completion work falls through the same callback into shared slot-1 gate `0x438d80`. |
| +0x180 | 0x0041f250 | CLTLoginMediator_DispatchCurrentHelperAuthMessage | - | - | tiny auth-side receive wrapper: reloads current helper from owner `+0x10` then tail-jumps to helper vtable `+0x10`; current best read is slot 5 / `AuthMessageDispatch`, reached from auth leaf dispatch `0x449a30` after base `0x442d00` leaves a message unconsumed |
| +0x184 | 0x0041f260 | CLTLoginMediator_DispatchCurrentHelperSlot6 | - | - | tiny mediator wrapper: jumps through current helper/state vtable `+0x14`; current best read is re-entry into the active state's slot-6 secondary-message handler |
| +0x188 | 0x0041afc0 | CLTLoginMediator_HandleMarginConnectionCompletionFallback | - | - | margin-connection fallback used from `CMarginConnection::OnOperationCompleted`: filters to owner `+0x1c`, clears owner `+0x1c/+0x20` on work type `1`, then re-enters current helper vtable `+0x04` (current best read: slot 2); on the restored existing-character path, because `+0xec -> 0x41b450(8)` immediately ran state8 slot3 and handed off into helper4, the later margin type-2 zero-status connect completion is expected to reach **state4 slot2** through this callback, not shared state8 slot2 |
| +0x18c | 0x0041e690 | CLTLoginMediator_FillState9CallbackBlob18c | - | - | state9-gated callback blob builder used indirectly by client `ClientNetShell +0x38 / 0x62006580`; fills fixed `0x20` bytes as: slot-record id low/high, caller args, then a 16-byte in/out region at `+0x10..+0x1f` seeded from owner `+0xf18` and transformed in place through mediator `+0xd4` (`owner +0x1c + 0x85`) via shared helper `0x41df60 / 0x44b190` / `FeedbackSize`; bounded assembly now shows `0x41e690` calls `+0xd4` directly with no alternate launcher-side seed branch, and replacement `+0x18c` now mirrors that tighter provenance too by sourcing the key through `+0xd4` alone instead of keeping an extra local fallback chain; the live connection `+0x85..+0x94` mirror is the real/original preferred source on the active path, while launcher-owned bootstrap sidecar reuse stays only as bounded fallback/diagnostic glue behind `+0xd4` until runtime is stable enough to prune it without risking game entry; current provenance consequence: owner `+0xf18` now traces back to state6 opcode-`9` success at `0x00440780`; see `VTABLES/0x004b508c.md` for field semantics and reply-layout detail |
| +0x190 | 0x0041f840 | CLTLoginMediator_AppendLateEntryStringTriple1470 | - | - | owner-side append wrapper for owner `+0x1470`, not `+0x818`; body is only `add ecx,0x1470` then jump to `0x41f640 = StringTripleArray_Append`; state6 opcode-`9` success uses this slot repeatedly after clearing `+0x1470`, pushing filename-like string-triple entries later exposed through arg6 `+0x118`; replacement source now keeps this wrapper thin too and mirrors the lower-level `0x41f640 / 0x41f3e0` helper family behind it |
