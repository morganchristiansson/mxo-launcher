# 0x004b508c - CLTLoginState_State6

## Overview
Family B concrete helper that sends a larger margin packet and then handles the follow-up reply. Allocated as an 8-byte object with two small state bytes.

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `CLTThreadPerClientTCPEngine_WorkItemHeader_GetWorkType`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x0043b8f0` | specialized code | builds and sends a margin packet with fixed constants, then posts event `0x11` |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x00440780` | `CLTLoginState_State6_Slot6_HandleMarginOpcode7Or9Reply` | handles opcode `7` / `9` replies, derives a key from `client.dll` + module path, and on opcode-`9` success writes owner `+0xf14/+0xf18` before driving the next state/event |
| 7 | `0x00438c70` | tiny state-id getter | undefined code in Ghidra; returns constant `6` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x004395f0` | constructor / initializer | installs vtable `0x004b508c` |

## Key specialized methods
- `0x0043b8f0` is the outbound step: after mediator/auth checks it fills a packet with several fixed constants plus current mediator values, sends it to margin, and posts event `0x11`.
  - packet layout now tightened further from the direct decompile:
    - `+0x00` = raw opcode `0x06` (`MS_ConnectRequest`)
    - `+0x01` = launcher version dword from owner `+0x08`
    - `+0x05` = client.dll version dword from owner `+0x0c`
    - `+0x09` = fixed byte `1`
    - `+0x0a` = fixed dword `0x11186887`
    - `+0x0e` = fixed dword `0x7460a4b0`
    - `+0x12..+0x21` = `GetGOBFileGUID` 16-byte LTGUID (`0x438e60` fallback family)
    - `+0x22` = owner current helper/state id byte
    - practical consequence for the restored existing-character path: after state4 slot2 zero-status success restores state8 slot3, the next natural outbound send is **not** state8 raw `0x0f` yet; state8 slot3 first gates on owner `+0xf14==0`, reaches state6 slot3, and this raw `0x06` send is the next outbound step before the later `0x07/0x09` continuation that eventually makes owner `+0xf14/+0xf18` live
  - newer Ghidra/disassembly recheck also fixes the slot-3 cache/write gate more tightly:
    - `0x43b8fc` tests state6 `this+4`
    - `0x43b904..0x43b91a` calls incoming upstream vtable `+0x18`
    - phase `4` or `5` preserves the existing cached upstream pointer
    - otherwise `0x43b91c` overwrites `this+4 = upstream`
    - `0x43b925..0x43b93b` gates on owner `+0x1c` ready-state and switches to helper `4` on failure
    - `0x43b943..0x43b959` then gates on `0x41b4d0` and switches to helper `5` on failure
  - newer original-launcher WineDbg on the active existing-character branch now fixes this method's timing more tightly:
    - it is reached immediately after the post-connect state8 re-entry (`0x4393f0 -> 0x43bd20 -> 0x43b8f0`)
    - on that first pass owner byte `+0xf14` is still `0`
    - `0x41b4b0` is already true there, but `0x41b4d0` then fails and `0x43b8f0` immediately switches to helper/state `5`
    - later, after state5 slot-3 and the local type-`0x0b` slot-2 continuation, the run returns to `0x43b8f0` again through `0x4395de`
  - practical consequence: the existing-character happy path does include state6 slot 3, but not as a one-shot direct bridge to the final restored state8 send
- `0x00440780` is the big differentiator for this object. On opcode `7` it builds a derived buffer using `client.dll` and the current module path; on opcode `9` it updates owner fields, clears owner `+0x1470`, pushes multiple filename-like string objects through owner slot `+0x190`, and then switches state based on the cached upstream object.
  - newer decompile-backed opcode-`7` tightening now closes the response builder much further:
    - payload `+0x01..+0x10` = 16-byte server seed
    - payload `+0x11..+0x14` = chunk-byte count for `0x43d800 = GenerateClientChunkHashes`
    - `0x43d800` MD5-hashes each chunk of two files:
      - `"client.dll"`
      - current process module path from `GetModuleFileNameA(NULL, ...)`
    - `0x4566a0` then MD5-folds `seed16 || chunkDigest0 || ... || chunkDigestN`
    - the opcode-`8` reply body is that final 16-byte digest
  - replacement source now mirrors that launcher-owned file-hash path instead of the older
    auth-key-MD5 shortcut
  - newer duplicate-flow recheck matters here too:
    - `0x44079b` tests `AX == 7`
    - the taken branch immediately enters the opcode-`7` parse/hash/send path at `0x4407a5`
    - current listing/decompile shows no bootstrap-phase one-shot guard, no seed/body compare, and
      no duplicate-challenge cache before it builds and sends raw `0x08`
    - practical fidelity consequence: do **not** invent a per-challenge dedupe rule for state6
      slot-6 just because repeated `0x07` traffic looks suspicious in replacement logs
  - newer `2026-03-21` tightening now bounds the opcode-`9` reply layout itself much better:
    - `0x0043a0b0 / 0x0043a060` build the reply view
    - `0x0043c950` in launcher.exe and `0x6253f000` in client.dll are the two current
      `"UDPSessionSecret:"` string users
    - that original debug-printer family labels the parsed opcode-`9` fields as:
      - `+0x01` = `Status`
      - `+0x05` = `GoHereAddr`
      - `+0x09` = `UDPSessionSecret`
      - `+0x0d` = offset to `MetrIds` array header `(u16 count, then u16 ids...)`
    - narrower static fact for this function:
      - parsed reply `+0x09` is the dword later copied into owner `+0xf18`
  - that same pass also answers the long-running owner `+0xf18` question positively:
    - on the opcode-`9` success side it writes:
      - owner byte `+0xf14 = 1`
      - owner dword `+0xf18 = parsedReply(+0x09)`
    - concrete assembly at `0x440ab9..0x440ac9`:
      - `mov eax,[0x004f78b8]`
      - `mov edx,[edi+0x9]`
      - `add eax,0xf14`
      - `mov [eax+4],edx`
      - `mov byte ptr [eax],1`
    - important split to keep explicit:
      - parsed reply `+0x09` feeds owner `+0xf18`
      - the immediate next helper-state target does **not** come from that field; `0x440acc..0x440ae0`
        reloads state6 `this`, reads cached upstream object `this+4`, and calls its vtable `+0x18`
    - practical consequence: this state6 slot-6 body is the first isolated real non-init launcher writer for owner `+0xf18`, and it lands before state8 slot-6 entry `0x43f930`
    - newer `+0x190` tightening also clarifies the neighboring late-entry list story:
      - `0x440a37` first clears owner `+0x1470` through `0x41f5f0`
      - then each metric id is mapped through `0x48ce00`
      - the resulting local string object is appended through owner slot `+0x190 = 0x41f840`
      - `0x41f840` itself writes owner `+0x1470`, not `+0x818`
      - later arg6 `+0x118 / 0x41af50` exposes that same `+0x1470` vector to client late-runtime consumers
    - newest replacement-side fidelity gain now mirrors that producer live enough on the active
      route too:
      - source now parses the opcode-`9` metric-id array itself
      - maps each id through the loaded client-side METR table (bounded stand-in for original
        launcher `0x48ce00 = MasterDatabase_MapMetricIdToFilename`)
      - fills owner `+0x1470` with the resulting filename strings before the later event-`0x18`
        callback
      - latest successful run populated 17 entries and still reached game
    - newer Ghidra/disassembly recheck now makes that restore tail explicit too:
      - `0x440ab9..0x440ac9` writes owner `+0xf18` then owner `+0xf14`
      - `0x440acc..0x440ae0` reloads state6 `this`, reads cached upstream object `this+4`, calls its vtable `+0x18`, and passes that result to `0x41b450`
      - `0x440ae5..0x440aed` posts event `0x12`
    - newer original-launcher WineDbg also confirms the later visible effect on the active branch:
      - `0x440780` is reached before the restored state8 sender returns
      - the later restored `0x43bd20` stop then shows owner byte `+0xf14 == 1`
      - and owner dword `+0xf18` already non-zero
      - caller chain there is `0x440ae5 -> 0x43bd20`, matching the static read that state6 slot-6 restores the cached upstream helper and returns to state8 slot 3
    - replacement go/no-go stays narrow:
      - do **not** synthesize `+0xf18` outside this writer path
      - a future faithful source-owned mirror is justified only on state6 opcode-`9` success, sourcing the original parsed reply `+0x09` dword rather than a placeholder value
      - that producer is now live in source for both:
        - owner `+0xf14/+0xf18`
        - owner `+0x1470` late-entry filenames
- Slot 7 (`0x00438c70`) returns `6`.

## Composition notes
- This is a very concrete login-state leaf: it owns both an outbound packet format and a complex reply parser.
- Its object layout (8 bytes with extra bytes reset in the dispatch-table init) matches the later fragment-collecting helpers such as `0x004b5104` and `0x004b5154`.
