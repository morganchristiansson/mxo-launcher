# VTable `0x004c05ac` - owner-linked delegating wrapper leaf on the `0x004c0540` base

## Current best read

This is another concrete owner-linked leaf in the `0x004c0540` cluster, but unlike `0x004c0588`
it is a **delegating wrapper** around a child/helper pointer stored at `+0x0c`.

High-confidence ctor facts from `0x481930`:

- sets `+0x04 = 1` (non-interlocked refcount)
- zeros `+0x08`, `+0x0c`, `+0x14`, `+0x1c`
- stores owner pointer at `+0x10`
- links itself through owner fields around `+0x298 / +0x2a4 / +0x2a0`

## Recovered rows

Direct memory read of `0x004c05ac`:

| Offset | Target | Current best read |
|---:|---:|---|
| `+0x00` | `0x0042f7e0` | shared plain `AddRef` helper |
| `+0x04` | `0x004830c0` | owner-aware release / unlink helper |
| `+0x08` | `0x004816f0` | shared getter over `+0x04` (`refcount04` here) |
| `+0x0c` | `0x004819f0` | delegate into child at `+0x0c`, vtable `+0x0c` |
| `+0x10` | `0x004819f0` | same delegate reused again |
| `+0x14` | `0x00441790` | no-op stub |
| `+0x18` | `0x00437b40` | shared tiny `return 0` stub |
| `+0x20` | `0x004830a0` | deleting dtor |

## Delegation proof

`0x4819f0` does:

```c
child = *(void **)(this + 0x0c);
child->vtable[3](child);
```

So both slots `+0x0c` and `+0x10` are currently just forwards into the child/helper object stored
at wrapper field `+0x0c`.

## Owner / release behavior

`0x4830c0` is not a plain decrement helper.
Its current best bounded behavior is:

- if `refcount04 == 1` and owner `+0x10` is non-null, call owner helper `0x482190(thisOwner, this)`
- then decrement `refcount04`
- zero -> deleting dtor at `+0x20`

So this wrapper participates in a separate owner-managed list/queue from the `0x004c0588` leaf.

## Destructor behavior

Deleting dtor `0x4830a0` runs concrete dtor `0x481990`, which:

- restores vtable `0x004c05ac`
- unlinks from its owner through helper `0x481200` when owner `+0x10` is live
- releases the delegated child object at `+0x0c` when present
- collapses back to base vtable `0x004c0540`

## Related docs

- `0x004c0540.md`
- `0x004c0564.md`
- `0x004c0588.md`
