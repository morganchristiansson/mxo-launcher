# 0x004b86f0 - `CStreamPacketEncryptionModule` **read helper** at owner `+0x04`

## Notes
- Current strongest construction anchor: `0x44da00` / `CStreamPacketEncryptionModule_ctor`.
- This helper is allocated by the margin-side lazy path reached from:
  - `0x441470 = CBaseMarginConnection_EnsureStreamPacketEncryptionModule`
  - called from `0x4429b0 = CBaseMarginConnection_HandleCode2CertChallengeAndSendResponse` on the
    consumed margin code-2 path
- parent object is now named from `0x44dab0` as:
  - `CStreamPacketEncryptionModule`
- hierarchy note:
  - this child table sits in the same helper-side family as common base `0x004b81c8`
  - it keeps common slot `+0x08 -> 0x44bb80` rather than the parent owner's string-backed
    `0x44dab0` override
- stronger role read from
  `0x469740 = CMessageConnectionPacketAgenda_InstallStreamPacketEncryptionModule`:
  - owner `+0x04` is inserted at agenda `+0x40`
  - that makes this the concrete module-side **read helper**
  - helper `+0x04` is a downstream helper-family link and initially points at the previous
    agenda read-chain head
  - owner `+0x10` flowing through this helper is the owning `CMessageConnection*`, which the
    transform body uses to snapshot connection `+0x24 = LTTCPEndpointKey`
    - `0x44aff0 = LTTCPEndpointKey_Copy` copies that 16-byte peer block to a local stack buffer
    - `0x44d500` then passes it into `0x44bca0 = CPacketDecryptor_DecryptPacket`
    - current strongest use of that peer block is discard/expiry logging on decrypt failure
- best current behavioral read:
  - the stored child collection built by `0x44d910` is the larger shared `FeedbackSize` transform-worker family reused on the auth-bootstrap path too
  - slot `+0x0c` (`0x44d500`, now `CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch`) allocates a fresh output message-ref, iterates that stored child collection, wraps each candidate in a `StreamTransformationFilter`, and tries each transform/decrypt entry in turn
  - the first successful transform with a non-empty output **replaces** the original message-ref and is dispatched through the downstream helper at wrapper `+0x04`
  - on total failure it clears wrapper `+0x0c` and dispatches a null result, so this helper can also **discard** the message
  - helper `+0x0c` is not just an opaque pointer:
    - `0x44d2e0` treats it as a small repeated-success control dword
    - with more than one child worker live, repeated success on worker index `0` increments that dword
    - after five such hits, `0x44d130` prunes the collection back down to only worker `0`
    - success on any later worker resets the control dword to `0`
  - the transformed output path is now tighter too:
    - `0x004b84f0` = final message-ref sink for transformed bytes
    - `0x004b8380` = read-side prefix-buffering sink sitting in front of that message-ref sink
  - current source now models this internal family as real C++ helper/worker classes used only by our code rather than raw ABI-exact structs, while keeping the original class/vtable facts documented here
  - slot `+0x10` (`0x44c670`) is the same tiny child delegate shape seen on `0x004b8690`
- practical significance for current launcher RE:
  - this is the missing module-side read helper that ties the recovered class family back into the packet-agenda read chain used by `CMessageConnection`

## VTable
| Offset | Function Address | Function Name | Instructions | Calls |
|--------|------------------|---------------|--------------|-------|
| +0x00 | 0x0044d980 | FUN_0044d980 | 16 | 1 |
| +0x04 | 0x0044bb60 | FUN_0044bb60 | 12 | 1 |
| +0x08 | 0x0044bb80 | FUN_0044bb80_ReturnStaticDescriptor4aafbb | 6 | 0 |
| +0x0c | 0x0044d500 | CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch | 210 | 23 |
| +0x10 | 0x0044c670 | FUN_0044c670 | 6 | 0 |
