# 0x004b86f0 Bootstrap challenge-material wrapper (outer child vtable)

## Notes
- Current strongest construction anchor: `0x44da00`.
- This wrapper is allocated by the bootstrap-side lazy path reached from `0x41470`, which itself is called from the decrypted-challenge continuation `0x4429b0`.
- Best current behavioral read:
  - slot `+0x0c` (`0x44d500`) iterates a stored child collection, tries to build/forward a message through each entry, and dispatches the first successful result through the callback object at wrapper `+0x04`
  - on total failure it clears wrapper `+0x0c` and dispatches a null result
  - slot `+0x10` (`0x44c670`) is the same tiny child delegate shape seen on `0x004b8690`
- Practical significance for current launcher RE:
  - this is additional evidence that the shared `+0x85` material is consumed through wrapper/helper objects, not just copied as raw bytes.

## VTable
| Offset | Function Address | Function Name | Instructions | Calls |
|--------|------------------|---------------|--------------|-------|
| +0x00 | 0x0044d980 | FUN_0044d980 | 16 | 1 |
| +0x04 | 0x0044bb60 | FUN_0044bb60 | 12 | 1 |
| +0x08 | 0x004b86f8 | No Name found | - | - |
| +0x0c | 0x0044d500 | FUN_0044d500 | 210 | 23 |
| +0x10 | 0x0044c670 | FUN_0044c670 | 6 | 0 |
