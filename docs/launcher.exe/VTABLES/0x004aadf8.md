# 0x004aadf8 - provisional `LauncherLoginDialog` subobject ctor-seed

## Focused block layout

`0x004aadf8` does not currently read like a stable long-lived vtable by itself.
It is better treated as a **ctor-seed mixed data block** immediately preceding the stable
subobject vtable at `0x004aae24`.

| Offset | Value | Current read |
|---|---:|---|
| +0x00 | `0x4028c0` | trivial ctor-seed stub |
| +0x04 | `0x3f400000` | float constant `0.75` |
| +0x08 | `0x3e800000` | float constant `0.25` |
| +0x0c | `0x4031d0` | `basic_string` scalar-dtor helper |
| +0x10 | `0x4031a0` | `basic_string` clear helper |
| +0x14 | `0x4031b0` | `basic_string` set helper |
| +0x18 | `0x4031c0` | `basic_string` get helper |
| +0x1c | `"basic_string"` | embedded RTTI/name-like text |
| +0x2c | `0x004aae24` | stable `LauncherLoginDialog + 0x54` secondary subobject vtable begins here |

## Current best identity

`0x004aadf8` is currently best read as a **ctor-only seed** for the same secondary subobject later
stabilized at:

- dialog `+0x54 -> 0x004aae24`

Current xrefs are extremely narrow:

- only `0x403390 = LauncherLoginDialog_ctor` writes `0x004aadf8`
- the same ctor immediately overwrites dialog `+0x54` with `0x004aae24`

## Practical reading

This means `0x004aadf8` is **not** currently good evidence for a separate long-lived top-level
class.

Instead, the tighter current read is:

- ctor seeds the `+0x54` subobject with `0x004aadf8`
- ctor then installs the stable secondary vtable `0x004aae24`
- dtor later restores only `0x004aae24`, not `0x004aadf8`

So `0x004aadf8` should presently be treated as:

- a temporary construction-stage vtable/data seed for the `LauncherLoginDialog` helper subobject
- not a stronger class-identity answer than `0x004aae28` + `0x004aae24`

## Open question

The exact reason for this temporary seed remains open, but current evidence is not strong enough to
promote it beyond a construction-stage note.
