# 0x004b81dc - common 3-slot **owner-side** base in the `CStreamPacketEncryptionModule` family

## Current best read
Latest cross-checks tighten this table to the owner branch, not the helper branch:
- `0x004b8704` (`CStreamPacketEncryptionModule`) sits directly on top of this base
- the child read/write helpers now read better through the separate 5-slot helper base
  `0x004b81c8`, not through this table

Current bounded interpretation:
- `0x004b81dc` = common owner-side 3-slot base
- `0x004b8704` = named owner/aggregator override on top of it

## Important slot `+0x08` restraint
It is tempting to call slot `+0x08` a common `GetClassName()` entry, but current evidence is only
strong for the named owner override:
- `0x004b8704 + 0x08 -> 0x44dab0`
  - returns literal string `"CStreamPacketEncryptionModule"`
- `0x004b81dc + 0x08 -> 0x44bb80`

`0x44bb80` is now confirmed as a real tiny function, but it returns a static descriptor pointer at
`0x4aafbb`, not a plain string literal.
So the safer current claim is:
- this owner-side base exposes a common slot `+0x08` interface
- but only the `0x004b8704` override is currently proven to be a plain class-name getter

## Recovered rows
| Offset | Function Address | Function Name | Notes |
|---:|---:|---|---|
| `+0x00` | `0x0044c6f0` | `FUN_0044c6f0` | owner-base destructor / releases `+0x04` and `+0x08`, clears `+0x0c` |
| `+0x04` | `0x0044bb90` | `CStreamPacketEncryptionModule_ReleaseChildren` | inherited directly by `0x004b8704` |
| `+0x08` | `0x0044bb80` | `FUN_0044bb80_ReturnStaticDescriptor4aafbb` | common owner-side virtual, meaning still unresolved |
