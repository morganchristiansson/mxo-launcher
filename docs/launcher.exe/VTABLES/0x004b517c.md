# 0x004b517c - CLTLoginState_State9

## Overview
Family B state-9 helper that consumes the result written into `DAT_004f788c` and pushes the pipeline toward the final leaf states. Allocated as an 8-byte object with a byte at `+4` and a word at `+6`.

Provisional better-name suggestion:
- `CLTLoginState_LoadCharacterFollowupPending`

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `LaunchPadClient_GetVtableOffset`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00439780` | specialized code | consumes the object's byte/word payload via `0x0041de40`, clears them, and posts event `0x17` on success |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x0043c180` | specialized code | handles opcode `0x11`; on success switches state `0x0c` and posts event `0x18` |
| 7 | `0x00438cc0` | tiny state-id getter | undefined code in Ghidra; returns constant `9` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437860` | tiny getter stub | undefined code in Ghidra; returns `1` |
| 10 | `0x00439750` | constructor / initializer | installs vtable `0x004b517c` |

## Key specialized methods
- `0x00439780` is very small but very specific: it consumes the state object's local byte/word payload, clears those fields, and posts event `0x17` if `0x0041de40` returns success.
- stronger current read on the owner helper `0x0041de40`:
  - owner callback/object triple `+0x84/+0x88/+0x8c` is installed by owner vtable `+0x124 /
    0x0041f1d0 = CLTLoginMediator_SetState9CallbackObjectTriple84_88_8c`
  - owner callback object `+0x84` fills two dwords through vtable `+0x38`
  - owner `+0x1c + 0x24..0x30` seeds a local transport/address block
  - `0x44afd0` / `0x44b0d0` derive a packet-like payload from helper word `+6`
  - owner object `+0x88` then chooses between:
    - direct send through vtable `+0x28`
    - or a handle-based path through `+0x1c`, `+0x18`, and `+0x24`
  - owner dword `+0x90` is only forwarded when helper byte `+4 != 0`
  - owner dword `+0x147c` caches the acquired handle on the managed-send branch
- source ownership note:
  - the scaffold keeps that byte/word payload on `CLTLoginState_State9` itself instead of smearing it onto the mediator
  - current source mirrors the local payload lifecycle in `CLTLoginState_State9::Slot3_BeginOrContinue`
  - helper11 `0x00440320` parsed word `+0x09` is now routed into state9 local `this+6`
  - current source also has a narrow mediator-owned `State9SubmitFollowupScaffold()` mirror for the
    `0x41de40` call shape, while leaving the deeper owner collaborators unresolved
- `0x0043c180` is the real reply handler. On opcode `0x11` success it updates owner state, calls owner vtable `+0x16c`, and explicitly switches state to `0x0c`, pushing execution into the next stage.
  - current source now mirrors the visible state transition and event/error shape in
    `CLTLoginState_State9::Slot6_HandleSecondaryMessage`
  - the owner vtable `+0x16c` side effect is now narrowed to `0x0041b420`:
    - clear owner byte `+0xf14`
    - set owner byte `+0x2d`
    - if margin connection `+0x1c` is in state `1` or `2`, call connection vtable `+0x0c(1)`
  - current source owns the evidence-backed owner-state mutation under the mediator while keeping
    the unresolved connection-vtable `+0x0c` collaborator explicit
- practical current read: state9 is now closed enough for the active path, with the remaining
  unresolved work pushed down to deeper owner/collaborator behavior rather than the state-vtable body itself
- Slot 7 (`0x00438cc0`) returns `9`, which matches the state entered by `0x00440320`.

## Composition notes
- This is a concrete intermediate node between state `11` (`0x004b5154`) and the final leaf-state family (`0x004b51b8` / `0x004b51e0` / `0x004b5208` / `0x004b5230`).
- The state transition to `0x0c` is especially important because one of the final leaves also returns state id `0x0c` in slot 7.
 slot 7.

