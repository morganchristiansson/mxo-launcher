# 0x004b5230 - CLTLoginState_State12

## Overview
Final-leaf variant with the strongest concrete identity in this last cluster: the dispatch table allocates it directly and sets byte `this+4 = 1`.

Current live-path relevance is stronger now:
- state9 slot 6 success (`0x0043c180`) switches to state `0x0c`
- this vtable is still the clearest concrete state-`0x0c` leaf candidate in the current family map
- representative natural-original UI around that later boundary was **Waiting for Regionserver**
- newer Ghidra-first tightening now also shows an important boundary between that success and any
  later hit on this leaf:
  - `0x43c180` success goes through `0x41b420`, then `0x41b450(0x0c)`, then `0x41cfb0(0x18)`
  - a later follow-on `0x41cfb0(0x0f)` was also live-observed before the run entered game
  - `0x41cfb0` walks the owner `+0x674` listener tree
- that still does **not** prove a concrete later hit on this vtable's slot bodies; treat it as the
  strongest current state-identity lead for the next pass, not as a settled immediate next stop
- newer follow-up late probes also keep this caution explicit:
  - no natural hit yet on shared final-leaf slot 6 `0x004397e0`
  - no natural hit yet on owner callback84 bridge `0x0041c5c0`

Provisional better-name suggestion:
- `CLTLoginState_FinalMarginLeaf12`
- keep this weak/provisional until a stronger semantic anchor appears

## VTable
| Slot | Entry Address | Kind / current symbol | Notes |
|---|---|---|---|
| 1 | `0x00438d80` | shared gate | common slot 1 handler; checks `CLTThreadPerClientTCPEngine_WorkItemHeader_GetWorkType`, then posts event/error via `DAT_004f78b8` |
| 2 | `0x00438df0` | shared gate | common slot 2 handler; same pattern as slot 1 but posts event `0x0f` / error `7` |
| 3 | `0x00441790` | tiny stub | no concrete slot-3 behavior |
| 4 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 5 | `0x004397c0` | tiny shared stub | undefined code in Ghidra; writes `DAT_004f78b8+0x80 = 0x12000004`, returns false |
| 6 | `0x004397e0` | specialized code | shared final-leaf handler; here the dispatch-table byte at `this+4` is preinitialized to `1` |
| 7 | `0x00438d00` | tiny state-id getter | undefined code in Ghidra; returns constant `12` |
| 8 | `0x00441790` | tiny stub | single `ret`; Ghidra sometimes mislabels this as `FUN_00401030`, but the vtable entry is `0x00441790` |
| 9 | `0x00437b40` | tiny getter stub | returns `0` |
| 10 | `0x00439d80` | shared initializer | same constructor target as the sibling leaves |

## Key specialized methods
- The biggest difference from `0x004b51e0` / `0x004b5208` is the dispatch-table init: this object is created with byte `+4 = 1`, which changes the behavior of shared slot 6 (`0x004397e0`) and lets it delegate to `0x0041c5c0`.
- Slot 7 (`0x00438d00`) is the tiny getter that returns `12`, which lines up neatly with the state transition into `0x0c` seen in `0x0043c180`.
- Practical next-pass breakpoint consequence from that read:
  - once `0x0043c180` success is proven, keep `0x004397e0` and `0x0041c5c0` as good **late probes**
    for the concrete post-state9 continuation
  - but prioritize `0x0041b450` / `0x0041cfb0` and their event-consumer side first, because the
    helper switch plus listener-tree event post is now statically proven between state9 success and
    any later final-leaf slot-6 hit
- Like the other final leaves, it still reuses the abstract-style constructor at `0x00439d80`.
- Focused source/doc homes for the current state-`0x0c` leaf identity:
  - `matrixstaging/game/src/libltclientlogin/loginstate_state12.cpp`
  - `../state_machine/POST_STATE9_CONTINUATION.md`

## Composition notes
- This is the clearest concrete “state `0x0c`” leaf under the `0x004b51b8` abstract-style node.
- Its existence is also good evidence that these objects are **`CLTLoginState` specializations** rather than broad `LaunchPadClient` subclasses.
