# 0x004b84f0 - final message-ref sink used by the helper-local transformed-output path

## Current best read
This table is now the clearest final output sink used by both:
- `0x44d500 = CStreamPacketEncryptionModule_ReadHelper_TransformAndDispatch`
- `0x44d390 = CStreamPacketEncryptionModule_WriteHelper_TransformAndDispatch`

Current strongest anchors:
- `0x44d500`
  - stack-local sink object is built around a freshly created message-ref
  - construction passes through temporary table `0x004b8438`
  - then switches to final table `0x004b84f0`
  - that sink is attached under `0x004b8380` on the read-helper transform path
- `0x44d390`
  - heap/pool sink object is built directly around a freshly created message-ref
  - final vtable is `0x004b84f0`
  - that sink is attached directly to `0x454310 = StreamTransformationFilter_ctor`

So current best role is:
- **message-ref output sink for transformed packet bytes**

## Recovered behavior
### Object shape
Confirmed field from constructor bodies:
- `+0x08` = retained output message-ref object
  - constructor AddRefs it when non-null
  - destructor Releases it when non-null

### Slot `+0x14` / `0x44c8b0 = CMessageConnectionMessageRefOutputSink_Put2`
Direct body shows:
- calls `0x00439820(this+0x08)` to obtain an append pointer inside the output message-ref
- memcpy-like copies the new bytes there
- then calls `outputMessageRef->vtable +0x18(length, 1)` to grow/commit the payload length

That is strong evidence this class is the concrete **byte sink writing into a `CMessageConnection` message-ref**.

### Slot `+0x20` / `0x44c8f0`
Direct body shows:
- local dword initialized to `0`
- calls `0x41bb60(outputMessageRef, &localZero)`

Current safest read:
- tiny output-length reset / flush-style helper on the same message-ref object
- keep the exact higher-level semantic conservative until the surrounding buffered-transformation base family is named more tightly

### Slot `+0x00` / `0x44d0f0 = CMessageConnectionMessageRefOutputSink_dtor`
- Releases field `+0x08` when present
- resets outer vtable to `0x004af29c`
- with delete flag bit 0 set, returns the object to freelist `DAT_004f804c`

That is the main practical distinction from sibling table `0x004b8438`, whose dtor uses generic free instead.

## Relationship to neighboring tables
Current best helper-local output chain:
- write helper path:
  - `0x004b84f0` sink receives transformed bytes directly
- read helper path:
  - `0x004b8380` buffers/strips the first 10 bytes
  - remaining transformed bytes then flow into `0x004b84f0`

## VTable highlights
| Offset | Function | Current best read |
|---:|---:|---|
| `+0x00` | `0x44d0f0` | `CMessageConnectionMessageRefOutputSink_dtor` | pooled dtor / releases output message-ref |
| `+0x14` | `0x44c8b0` | `CMessageConnectionMessageRefOutputSink_Put2` | append transformed bytes into output message-ref |
| `+0x20` | `0x44c8f0` | `FUN_0044c8f0` | tiny message-ref reset/flush-style delegate |
